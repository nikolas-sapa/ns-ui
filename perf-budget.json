{
  "// generated": "npm run test:perf-budget -- --update — see scripts/test-perf-budget.ts",
  "// tolerance": "CI fails a value exceeding budget by more than 10%.",
  "// scope": "Client JS weight only. Says nothing about TTFB, paint, or INP.",
  "// baselined-from": "Fixed largestChunkKb (2619->1778): demos[name] selection moved from a Server Component into a Client Component (app/_components/demo-lazy.tsx) so Turbopack code-splits each of the 389 registry demos into its own async chunk instead of bundling all of them eagerly into one ~3.1MB chunk shipped on every /preview/<name> and /preview/<name>/embed request. totalClientJsKb (5984->7046) is NOT fixed by that change and does not need to be: it is genuine growth from 63 new components' demo code (previously undercounted because bundling many demos together let them share overhead now spent once per real chunk) plus small per-chunk wrapper overhead from finer splitting. Re-baselined deliberately per the growth being real, not to silence a red largest-chunk failure, which the fix already resolved.",
  "totalClientJsKb": 7046,
  "largestChunkKb": 1778
}
