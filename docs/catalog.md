# Component Catalog — deep crawl 2026-07-18

1535 components enumerated from 14 sites (full-catalog crawl, not landing pages).

## Category map

### text (260)
- Dynamic Weight — per-letter variable-font weight morph by cursor proximity (originkit.dev)
- Inkbleed — cursor-reactive gooey ink-bleed text with liquid character merging (originkit.dev)
- Fluid Text — chars distort/scale by mouse proximity while total width stays constant (satisui.xyz)
- Apple Hello Effect — SVG handwriting animation in 4 language variants (chanhdai.com)
- Line Stitch — stitched text unraveled by touch (framer.com)
- Text Vaporize — words dissolve into particles and reassemble into the next word (originkit.dev)
- Moiré — interference-pattern shimmer through letterforms, pure CSS (text-effects.colorion.co)
- Random Letter Swap — glyphs swap vertically on hover in random order (originkit.dev)

### background (89)
- Black Hole — 3D black hole with orbiting particles and glowing event horizon (originkit.dev)
- Thunder Strike — customizable WebGL lightning shader (originkit.dev)
- Caustic Glass — shifting light pools refract beneath frosted glass (kinetics.colorion.co)
- Warp Grid — planar field bends inward around its focal point (kinetics.colorion.co)
- Character Waves — ASCII field driven by layered noise, ripples from cursor (originkit.dev)
- Chromatic Waves — rainbow Perlin-noise halftone dot-matrix in WebGL (originkit.dev)
- Prisma Nodes — interactive fiber node network (framer.com)
- Watercolor Shader — watercolor-inspired shader background (framer.com)

### cursor (46)
- Fluid Trail — GPU Navier-Stokes fluid simulation swirling behind the cursor (originkit.dev)
- Elastic Lasso — drag a selection field across a dot constellation (kinetics.colorion.co)
- Axis Cursor — precision crosshair with live X/Y coordinate readout (originkit.dev)
- Clipped Circle — cursor-following soft spotlight circle inside a container (ui.unlumen.com)
- inverted-cursor — blend-mode cursor inverts content beneath (21st.dev)
- smokey-cursor-effect — fluid smoke simulation trails the pointer (21st.dev)
- User Cursor — spring-tracked arrow with trailing name pill, Figma-style (originkit.dev)
- Pixel Cursor Trail — interactive pixel image reveal trail (framer.com)

### 3d (70)
- Sticker Peel — image sticker peels and curls away on hover/press (originkit.dev)
- Image Fold — photo unrolls like a rolled-up scroll via WebGL shader (originkit.dev)
- Wax Seal — pour, stamp and emboss in 3D wax (framer.com)
- Box Carousel — media rotates across the four faces of a 3D cube (originkit.dev)
- Draggable Sticker — WebGL peel curl with velocity tilt, sheen, settle-back (originkit.dev)
- cobe-globe family — 13 dotted WebGL globe variants: flights, satellites, live data (21st.dev)
- 3D Gallery Tunnel — four path modes with autoplay (framer.com)
- Round Carousel — draggable 3D cylinder gallery with momentum spin (originkit.dev)

### scroll (65)
- Scroll Island — dynamic-island element reacting to scroll (ui.watermelon.sh)
- Radial Scroll Gallery — scroll rotates items along a large partially hidden circle (satisui.xyz)
- Pinned Card Fan — pins to viewport and fans children out in an arc on scroll (satisui.xyz)
- Pixel Unfold — image revealed tile by tile on scroll (originkit.dev)
- Fan Out — photo deck dealt by scroll (framer.com)
- Volumen Scroll Video — scroll becomes the video scrubber (framer.com)
- scroll-image-tunnel — scroll flies through an image tunnel (21st.dev)
- CMS Scroll Blinds — scroll blind transitions between images (framer.com)

### card (100)
- Pixel Card — shimmering pixel grid grows in from center or any edge on hover (originkit.dev)
- Polarized Foil — muted spectrum appears only at grazing angles (kinetics.colorion.co)
- Receipt Pricing Card — receipt-style reveal pricing card (framer.com)
- Team Stack — stacked deck fans out on hover with spring CTA pills (ui.unlumen.com)
- Gliding Card — floating card glides between list items via shared hover state (satisui.xyz)
- Glow Card Grid — glowing border and background hover effects (chanhdai.com)
- Swipe Stack — fanned 3D stack that tilts and cycles to the back on swipe (originkit.dev)
- glass3d effect — layered glassmorphism with noise texture and inset edge highlights (glass3d.dev)

### button (77)
- Hold to Confirm — press and hold, ring fills, release early to cancel (kinetics.colorion.co)
- The Eclipse — magnetic-pull button with eclipse color-inversion on hover (satisui.xyz)
- Slide to Unlock — draggable iPhone-classic unlock gesture with shimmer text (chanhdai.com)
- Keycap Press — mechanical keycap depresses on its shadow (kinetics.colorion.co)
- Micro Expander — circular icon button expands into a text pill on hover (satisui.xyz)
- Emoji Burst — gravity-driven emoji particle burst on click with springy shake (originkit.dev)
- Prime Button — async button managing loading/success/error with layout animations (satisui.xyz)
- IO Knob — interactive rotary knob control (framer.com)

### nav (116)
- TOC Minimap — compact hoverable table-of-contents minimap with active tracking (chanhdai.com)
- Orbital Action Menu — actions escape a magnetic centre on hover (kinetics.colorion.co)
- Gooey Navbar Menu — gooey blob transition between nav items (ui.unlumen.com)
- tubelight-navbar — glowing tubelight active indicator (21st.dev)
- Contextual Dock — nearby dock controls swell in a soft focus field (kinetics.colorion.co)
- Line Nav — vertical nav with line marker expanding on hover/active (chanhdai.com)
- Sliding Capsule Nav — fluid layout animations with z-indexed capsule layering (satisui.xyz)
- shifting-dropdown — dropdown panel morphs between tabs (21st.dev)

### chart (56)
- Dither Area Chart — dithered terminal-native area charts with bloom and stacking (termcn.dev)
- Packet Trace — data pulses choose a route through a tiny network (kinetics.colorion.co)
- Phase Lock — three independent signals resolve into sync (kinetics.colorion.co)
- Heartbeat Monitor — glowing dot traces an EKG along the path (kinetics.colorion.co)
- GitHub Contributions — year-long contribution heatmap with tooltips and themes (chanhdai.com)
- Compare Ring — comparison arcs that fill, dots that race (framer.com)
- ai-swarm-visualization — animated AI agent swarm graph (21st.dev)
- Math Graph — animated math function graph plot (ui.unlumen.com)

### other (656)
- Metaball Bridge — two particles trade mass through a liquid neck (kinetics.colorion.co)
- Matrix — LED dot-matrix display with radial glow, presets and VU meter mode (ui.unlumen.com)
- Gooey SVG Filter — metaball blob effect via SVG filter (ui.unlumen.com)
- Liquid Distortion — real-time WebGL fluid ripples on any image (originkit.dev)
- Juice Effect — image silhouette filled with gooey liquid particles fusing into blobs (originkit.dev)
- Infinity Canvas — infinitely zoomable draggable image gallery with momentum (originkit.dev)
- Gravity Gallery — images dropped into a Matter.js physics world with drag tossing (originkit.dev)
- Halftone Media FX — real-time halftone and dithering system for media (framer.com)

## Novel finds (missed by the landing-page pass)

1. **`dynamic-weight-text`** (core) — Each letter morphs between two variable-font weights based on cursor proximity — pure typography interaction, no color or particles. Perfect Geist Sans fit. _[originkit.dev (Dynamic Weight / Weight Hover); also 21st.dev variable-font-and-cursor]_
2. **`moire-lens`** (core) — Counter-rotating line fields create an impossible-depth moiré illusion — subtle, monochrome, hypnotic on a dark base. _[kinetics.colorion.co (Moiré Lens)]_
3. **`metaball-bridge`** (core) — Two dots trade mass through a liquid neck — a tiny gooey physics vignette usable as loader or state-transfer indicator. _[kinetics.colorion.co (Metaball Bridge)]_
4. **`packet-trace`** (core) — Data pulses choose routes through a tiny node network — dev-tool aesthetic status/activity viz, dark minimal native. _[kinetics.colorion.co (Packet Trace; siblings Phase Lock, Checksum Bloom)]_
5. **`hold-to-confirm`** (core) — Press-and-hold destructive action: a ring fills while held, release early to cancel. Real UX value, not just decoration. _[kinetics.colorion.co (Hold to Confirm)]_
6. **`inertial-dial`** (core) — Rotary control where a weighted needle lags and catches up after the ring turns — tactile physics without flash. _[kinetics.colorion.co (Inertial Dial; Rotary Knob sibling)]_
7. **`led-matrix-display`** (core) — LED dot-matrix panel with SVG pixels, radial glow, animated presets and a VU-meter mode — hardware-nostalgia widget distinct from terminal/ASCII effects. _[ui.unlumen.com (Matrix)]_
8. **`scroll-island`** (core) — Dynamic-island pill that morphs and reacts to scroll position — iOS-grade chrome for a minimal header. _[ui.watermelon.sh (Scroll Island)]_
9. **`handwriting-hello`** (core) — SVG stroke handwriting draw-on animation (Apple 'hello' style) — elegant signature moment for hero or 404. _[chanhdai.com (Apple Hello Effect); 21st.dev hand-writing-text]_
10. **`fluid-trail`** (loud) — GPU Navier-Stokes fluid simulation swirling and glowing behind the cursor — real physics, not an image trail. _[originkit.dev (Fluid Trail); 21st.dev smokey-cursor]_
11. **`sticker-peel`** (loud) — Image behaves as a physical sticker: WebGL peel curl, velocity tilt, sheen, springs back on release — draggable variant included. _[originkit.dev (Sticker Peel / Draggable Sticker)]_
12. **`image-fold-unroll`** (loud) — Photo unrolls like a rolled-up scroll via a 3D WebGL shader, scroll-triggered or on load. _[originkit.dev (Image Fold)]_
13. **`electric-border`** (loud) — Crackling noise-displaced electric outline with glow wrapping any frame — strong CTA/highlight treatment. _[originkit.dev (Electric Border)]_
14. **`inkbleed-text`** (loud) — Cursor-reactive gooey ink-bleed typography: liquid blur makes neighboring characters merge and separate. _[originkit.dev (Inkbleed)]_
15. **`mesh-text-drag`** (loud) — Text warped across a WebGL mesh that drags with the cursor and springs back — cloth-like kinetic type. _[originkit.dev (Mesh Text Hover)]_
16. **`fluid-width-text`** (loud) — Characters distort and scale by mouse proximity while total line width stays constant — the compensation constraint is the novel trick. _[satisui.xyz (Fluid Text)]_
17. **`wax-seal`** (loud) — Pour, stamp and emboss a 3D wax seal — one-of-a-kind confirmation/signature interaction, nothing else in any catalog resembles it. _[framer.com marketplace (Wax Seal)]_
18. **`line-stitch-text`** (loud) — Text rendered as embroidered stitching that unravels under touch/cursor — tactile destruction-and-repair typography. _[framer.com marketplace (Line Stitch)]_

---

## Per-site coverage

### https://transitions.dev/ — 33 components
_Full catalog enumerated. Site is a single-page library: tavily_map found only /, detail.html?t=<slug>, refine.html, skill.html (plus pro.html, privacy, terms). The homepage grid lists all transitions with names and one-line descriptions; extraction captured 33 items, consistent with the Pro page's stated "33+ transitions / full 30-transition library". Detail pages (e.g. card-resize) repeat the same catalog plus implementation notes (plain CSS, motion tokens). Non-component pages: Refine (live motion-tuning tool for agents), Skill (AI agent skill), Pro (paid tier with React/TS variants). No auth wall blocked enumeration; only Pro code snippets are gated, not the component list._

- **Card resize** `card` — Smooth card resize transition between sizes
- **Number pop-in** `text` — Digit flip with blur and stagger
- **Notification badge** `other` — Badge diagonal slide-in with spring pop
- **Text states swap** `text` — Text swap transition with blur between states (e.g. processing)
- **Menu dropdown** `nav` — Origin-aware dropdown open/close transition
- **Confetti burst** `button` — Physics-based confetti that lands on the button
- **Modal open/close** `other` — Modal transition with scale
- **Panel reveal** `other` — Panel open/close transition
- **Page side-by-side** `nav` — Forward/back page slide transition
- **Icon swap** `button` — Scale and blur icon swap
- **Success check** `button` — Success checkmark with blur and rotate
- **Avatar group hover** `other` — Distance-falloff lift with bouncy return across avatar row
- **Card stack hover** `card` — Card stack fans out with a spring on hover
- **Error state shake** `other` — Cubic-bezier shake on input error
- **Input clear with dissolve** `other` — Input clears with per-word dissolve
- **Skeleton loader and reveal** `other` — Skeleton pulse to content cross-fade
- **Texts reveal** `text` — Two text lines rise with offset stagger
- **Tabs sliding** `nav` — Pill indicator follows the active tab
- **Shimmer text** `text` — Masked gradient sweep across text
- **Organic shimmer** `text` — Wavy shimmer with phase-locked edge glow
- **Tooltip open/close** `other` — Tooltip with appear-only delay, instant exit
- **3D tilt** `3d` — 3D pointer tilt on a card with cursor glare (credit-card demo)
- **Dropdown menu morph** `nav` — Button morphs into a menu surface
- **Accordion** `other` — Grid-rows height animation with chevron morph
- **Toast open/close** `other` — Toast rises in with fade, blur and scale
- **Like button** `button` — Heart fills and bursts particles
- **Image open tilt** `3d` — iPadOS-style image zoom with 3D tilt and bend
- **Learn more hover** `button` — Chevron shifts and opens on hover
- **Checkbox check** `button` — Checkmark draws on with a stroke path
- **Spinning counter** `text` — Digits spin like a reel to the target value
- **Toggle** `button` — Toggle thumb slides with a double bounce
- **Pro gradient text** `text` — Colour washes orbit the letters (Pro)
- **Delete with smoky dissolve** `other` — Image shreds and falls into smoke on delete (Pro)

### https://www.termcn.dev/docs/components — 120 components
_Full catalog enumerated from the site's own machine-readable index (termcn.dev/llms.txt), which lists every docs page: 91 components across 11 groups (Layout, Typography, Input, Selection, Data, Feedback, Navigation, Overlays, Forms, Utility, AI), 12 charts, and 9 templates. Note: termcn is a TERMINAL UI library (Ink + OpenTUI React components, shadcn-CLI compatible), not web effects — so categories like background/cursor/3d do not apply. Nearly all components ship in both Ink and OpenTUI variants with identical descriptions; deduplicated here, with the two Ink-only AI components (Conversation History, Error Retry) marked. The site also ships 41 terminal color themes per framework (Dracula, Catppuccin, Nord, Tokyo Night, etc.), omitted as themes rather than components. Verified against the rendered sidebar and two live component pages (Bar Chart, Tool Call)._

- **Aspect Ratio** `layout` — Constrains children to a given aspect ratio
- **Box** `layout` — Enhanced flexbox container with theme-aware borders
- **Center** `layout` — Centers children horizontally and vertically
- **Columns** `layout` — Multi-column layout with configurable widths
- **Divider** `layout` — Horizontal or vertical divider/rule
- **Grid** `layout` — Rows x columns grid layout
- **Scroll View** `scroll` — Scrollable container with scrollbar
- **Spacer** `layout` — Flexible space between flex children
- **Stack** `layout` — Vertical/horizontal stack with gap
- **Badge** `text` — Semantic status indicator badge (success/error/warning/info)
- **Big Text** `text` — Figlet-style large ASCII art text
- **Code** `text` — Syntax-highlighted code block (50+ languages)
- **Digits** `text` — Box-drawing character numerals
- **Gradient** `text` — Color gradient text
- **Heading** `text` — h1-h4 styled headings with optional figlet ASCII art
- **Link** `text` — OSC 8 clickable hyperlink
- **Markdown** `text` — Full markdown renderer for terminal
- **Tag** `text` — Removable chip/tag
- **Email Input** `input` — Email input with validation and domain suggestions
- **Masked Input** `input` — Masked input for phone, date, credit card formats
- **Number Input** `input` — Numeric input with increment/decrement and bounds
- **Password Input** `input` — Password input with masked characters and reveal toggle
- **Path Input** `input` — Filesystem path input with tab autocomplete
- **Search Input** `input` — Fuzzy search input with autocomplete
- **Text Area** `input` — Multi-line text editor with word wrap
- **Text Input** `input` — Single-line text input with placeholder, validation, mask
- **Checkbox** `input` — Checkbox with indeterminate state
- **Checkbox Group** `input` — Checkbox group with min/max selection
- **Color Picker** `input` — Color picker with palette, hex, and RGB input
- **Multi Select** `input` — Multi-select list with checkboxes and select-all
- **Radio Group** `input` — Mutually exclusive radio button group
- **Select** `input` — Single-select dropdown with arrow-key navigation
- **Tag Input** `input` — Add and remove tags inline
- **Tree Select** `input` — Hierarchical tree selection
- **Card** `card` — Card with header, body, and footer slots
- **Data Grid** `data` — Advanced data grid with sorting, filtering, pagination, and cell editing
- **Definition** `data` — Term-description definition list
- **Diff View** `data` — Unified, split, and inline diff viewer with LCS diff algorithm and line numbers
- **Directory Tree** `data` — Filesystem browser with expand/collapse
- **Git Status** `data` — Git status display (branch, staged, modified, ahead/behind)
- **JSON** `data` — Pretty-printed JSON with collapsible nodes
- **Key Value** `data` — Aligned key-value pairs
- **List** `data` — Navigable list with optional filtering
- **Table** `data` — Sortable, selectable, paginated data table
- **Tree** `data` — Expandable/collapsible tree view
- **Virtual List** `data` — Virtualized list for 10k+ items
- **Alert** `feedback` — Alert box with success/error/warning/info variants
- **Banner** `feedback` — Full-width announcement banner
- **Multi Progress** `feedback` — Parallel progress display for package managers, builds, deploys
- **Progress Bar** `feedback` — Determinate progress bar with percent and value/total display
- **Progress Circle** `feedback` — Circular progress indicator using Unicode braille
- **Skeleton** `feedback` — Shimmer loading placeholder
- **Spinner** `feedback` — Animated spinner with 12+ styles
- **Status Message** `feedback` — Inline status message with icon and color
- **Toast** `feedback` — Auto-dismissing toast notification
- **Breadcrumb** `nav` — Breadcrumb navigation trail
- **Command Palette** `nav` — VS Code-style command palette with fuzzy search
- **Menu** `nav` — Dropdown menu with nested submenus
- **Pagination** `nav` — Page navigation with prev/next and jump
- **Sidebar** `nav` — Collapsible navigation sidebar panel
- **Tabbed Content** `nav` — Tab bar wired to content panels
- **Tabs** `nav` — Tab bar with arrow and tab-key navigation
- **Dialog** `overlay` — Confirmation dialog with OK/Cancel actions
- **Drawer** `overlay` — Slide-in panel from any edge
- **Modal** `overlay` — Focus-trapped overlay modal, close with Esc
- **Notification Center** `overlay` — Persistent queue-based notification panel with useNotifications hook
- **Popover** `overlay` — Positioned popover with arbitrary content
- **Tooltip** `overlay` — Contextual tooltip on focus or hover
- **Confirm** `form` — Yes/No confirmation prompt
- **Date Picker** `form` — Calendar date picker with keyboard navigation
- **File Picker** `form` — Interactive file browser and picker
- **Form** `form` — Form container with validation, Ctrl+S submit, and dirty tracking
- **Form Field** `form` — Form field wrapper with label, hint, and error
- **Time Picker** `form` — Time picker with hour/minute/second spinners
- **Wizard** `form` — Multi-step wizard with per-step validation
- **Clipboard** `button` — Copy-to-clipboard button with success feedback
- **Clock** `utility` — Real-time clock display with 12h/24h and date
- **Embedded Terminal** `utility` — Embedded PTY terminal panel (requires node-pty peer)
- **Error Boundary** `utility` — React error boundary with graceful terminal display
- **Help** `utility` — Auto-generated help panel from a keymap
- **Image** `utility` — Image renderer with iTerm2/Kitty protocol and ASCII fallback
- **Keyboard Shortcuts** `utility` — Formatted keyboard shortcut reference table
- **Log** `utility` — Scrolling log viewer with severity levels and filtering
- **Panel** `utility` — Titled bordered panel
- **QR Code** `utility` — Unicode block QR code generator
- **Stopwatch** `utility` — Count-up stopwatch with laps and reset
- **Timer** `utility` — Countdown timer with pause/resume and auto-start
- **Toggle** `button` — Toggle/switch component with on/off labels
- **Chat Message** `ai` — Chat message bubble with role label, streaming indicator, collapsible content
- **Chat Thread** `ai` — Scrollable container stacking ChatMessage components into a conversation
- **Conversation History** `ai` — Arrow-key scrollable stack for long chat threads (Ink only)
- **Error Retry** `ai` — Inline error callout with retry and dismiss shortcuts (Ink only)
- **File Change** `ai` — File-change review list with diff expansion and per-file accept/reject
- **Model Selector** `ai` — Keyboard-navigable AI model list with provider grouping and context-window display
- **Streaming Text** `ai` — Token-by-token streaming text with blinking cursor and typing animation
- **Thinking Block** `ai` — Collapsible block for AI extended thinking/reasoning content
- **Token Usage** `ai` — Token counter with cost estimate; exports ContextMeter for context-window fill
- **Tool Approval** `ai` — Approval prompt for AI tool calls with risk badge, args, auto-deny timeout
- **Tool Call** `ai` — AI tool invocation with status indicator, args, result, elapsed time
- **Bar Chart** `chart` — Horizontal and vertical bar chart with labels
- **Line Chart** `chart` — ASCII line chart with axes and multi-series support
- **Pie Chart** `chart` — Unicode block pie chart with legend
- **Sparkline** `chart` — Inline Unicode braille sparkline chart
- **Gauge** `chart` — Arc/speedometer gauge meter
- **Heat Map** `chart` — Grid heatmap with color intensity scale
- **Dither Area Chart** `chart` — Composable dithered area charts with layered fills, axes, stacking, bloom, interaction
- **Dither Bar Chart** `chart` — Dithered grouped/stacked/percent bar charts with terminal-native textures
- **Dither Line Chart** `chart` — Dithered multi-series line charts with solid/dashed strokes and markers
- **Dither Pie Chart** `chart` — Dithered pie and donut charts with textured slices, legends, tooltips
- **Dither Radar Chart** `chart` — Dithered radar charts comparing series across radial axes
- **Dither Sparkline** `chart` — Compact dithered sparklines for dashboards and inline metrics
- **App Shell (template)** `template` — Full-screen TUI layout with header, tip bar, input, scrollable content, hints footer
- **Bullet List (template)** `template` — Nested structured content with bullet/tree prefixes
- **Help Screen (template)** `template` — Figlet banner + tagline + aligned Options/Commands help sections
- **Info Box (template)** `template` — Bordered info panel with header, key-value rows, tree-prefix rows
- **Login Flow (template)** `template` — Full-page onboarding/login screen with banner, big ASCII title, numbered select
- **Setup Flow (template)** `template` — @clack-style step flow with diamond/pipe visual language and multi-select
- **Splash Screen (template)** `template` — Startup banner with big ASCII art title, subtitle, author credit, status line
- **Usage Monitor (template)** `template` — Real-time dashboard with progress metrics, burn rate, predictions, status bar
- **Welcome Screen (template)** `template` — Two-panel welcome dashboard with titled border, logo, greeting, sections

### https://ui.unlumen.com/components — 76 components
_Full catalog enumerated: /components gallery index (50 unique items across Animations 26, Image Effects 2, Marketing 4, Navigation 8 with Motion Tabs Menu double-listed, Backgrounds & Shaders 7, Icons 4) plus the Primitives docs index at /docs/ui (26 items: 20 Unlumen UI, 3 Buttons, 3 Effects), cross-checked against a tavily_map site crawl. Descriptions verified from component pages for Matrix, Pinned List, Side By Side Slide, Dia Text Reveal, Team Stack, Clipped Circle; remaining descriptions inferred from names and index/category metadata, not fabricated features._

- **Dia Text Reveal** `text` — Text reveal with colorful gradient sweep animating across the letters (Dia-app style)
- **Motion Tabs Menu** `nav` — Tab menu with smoothly animated sliding active indicator
- **Team Stack** `card` — Stacked deck of team member cards that fans out on hover/tap with spring CTA pills
- **Hover Feature Cards** `card` — Feature cards with animated hover states
- **Stacked Feature Cards** `card` — Feature cards stacked in a pile with animated transitions
- **Motion FAQs Accordion** `other` — FAQ accordion with smooth motion open/close transitions
- **Video Slider** `other` — Animated slider/carousel for video content
- **Cursor Image Trail** `cursor` — Trail of images that follows the mouse cursor
- **Hover Image List** `other` — Text list that reveals floating images on hover
- **Tilt Card** `card` — Card that tilts in 3D perspective following the cursor
- **Hover Expand** `other` — Row of items/images that expand on hover
- **Gooey SVG Filter** `other` — Metaball/gooey blob effect using an SVG filter
- **Vercel Snap Text** `text` — Text animation with snap-style transitions (Vercel-inspired)
- **File Tree** `nav` — Animated expandable/collapsible file tree view
- **GitHub Graph** `chart` — GitHub-style contribution heatmap grid
- **Animated List** `other` — List whose items animate in/out with motion transitions
- **Favicon Search** `other` — Search input that displays site favicons for results
- **Animate Digits** `text` — Number digits that roll/animate when values change
- **Smart Animate Text** `text` — Text that smart-animates between different strings (Figma-style)
- **Vercel Animate Count** `text` — Animated count-up number in Vercel style
- **Apple Switch** `button` — iOS-style animated toggle switch
- **Pinned List** `other` — List items that animate smoothly into a pinned section via layout animations
- **Matrix** `other` — LED dot-matrix display with SVG pixels, radial glow, animated presets and VU meter mode
- **AI Chat** `other` — Animated AI chat interface component
- **Youtube Video Ambient** `background` — Ambient glow background bleeding from video content (YouTube-style)
- **Side By Side Slide** `other` — Before/after image comparison with spring-animated draggable divider using clip-path
- **Orbital Image Wheel** `3d` — Images arranged and rotating on an orbital wheel
- **Horizontal Depth Fade** `other` — Horizontal image strip with depth-fade at the edges
- **Inline Testimonials** `card` — Testimonial quotes shown inline with animated transitions
- **Aurora Card** `card` — Card with animated aurora gradient glow
- **Blob Card** `card` — Card with animated organic blob background
- **Vertical Marquee** `scroll` — Vertically auto-scrolling marquee of content
- **Dock** `nav` — macOS-style dock with magnifying icon animation
- **Motion Navigation Menu** `nav` — Navigation menu with animated dropdown/indicator motion
- **Gooey Navbar Menu** `nav` — Navbar menu with gooey blob transition between items
- **Floating Navbar** `nav` — Navbar that floats and animates on scroll
- **Expandable Navbar** `nav` — Navbar that expands/collapses with animation
- **Sidebar 001** `nav` — Animated collapsible sidebar layout (variant 1)
- **Sidebar 002** `nav` — Animated collapsible sidebar layout (variant 2)
- **Gravity Stars** `background` — Starfield background with gravity-attracted particle motion
- **Pixel Liquid Background** `background` — Liquid-like animated pixel shader background
- **Aurora Bars** `background` — Background of glowing aurora-colored animated bars
- **Pixel Background** `background` — Animated pixelated background pattern
- **Pixel Scroll Transition** `scroll` — Pixelation transition effect driven by scroll
- **Wave Background** `background` — Animated wave shader background
- **Square Pattern** `background` — Animated square grid pattern background
- **Sidebar Toggle Icon** `other` — Animated icon morphing for sidebar open/close
- **List View Icon** `other` — Animated list-view toggle icon
- **Compact View Icon** `other` — Animated compact-view toggle icon
- **Cards View Icon** `other` — Animated cards-view toggle icon
- **Command Menu (primitive)** `nav` — Animated cmd+K command palette
- **Cursor (primitive)** `cursor` — Custom animated cursor replacement
- **Floating Tooltip (primitive)** `other` — Tooltip that floats and follows the cursor
- **Glowing Badge (primitive)** `other` — Badge with animated glow ring
- **Highlight (primitive)** `text` — Animated marker-style text highlight
- **Kbd (primitive)** `other` — Styled keyboard-key cap element
- **Magnetic Button (primitive)** `button` — Button that magnetically attracts toward the cursor
- **Orbiting Skills (primitive)** `3d` — Icons orbiting around a central element
- **Progressive Blur (primitive)** `other` — Gradient progressive blur overlay for edges/scroll areas
- **Scramble Text (primitive)** `text` — Text that scrambles/decodes character by character
- **Shimmer Skeleton (primitive)** `other` — Loading skeleton with shimmer sweep
- **Shimmering Text (primitive)** `text` — Text with animated shimmer/sheen sweep
- **Slider (primitive)** `other` — Animated range slider control
- **Switch (primitive)** `button` — Animated toggle switch control
- **Text Reveal (primitive)** `text` — Text revealed with staged motion animation
- **Theme Switch (primitive)** `button` — Animated light/dark theme toggle
- **Tilt Card (primitive)** `card` — Card with cursor-driven 3D tilt
- **Tilt (primitive)** `3d` — Generic 3D tilt wrapper for any content
- **Tooltip Preview (primitive)** `other` — Tooltip showing a rich preview on hover
- **Velocity Highlight (primitive)** `text` — Text highlight that reacts to scroll velocity
- **Copy Button (primitive)** `button` — Copy-to-clipboard button with animated confirmation
- **Glow Button (primitive)** `button` — Button with animated glow effect
- **Refresh Button (primitive)** `button` — Refresh button with spinning icon animation
- **Clipped Circle (primitive)** `cursor` — Cursor-following soft glowing spotlight circle inside a container
- **Count Up (primitive)** `text` — Animated number count-up effect
- **Math Graph (primitive)** `chart` — Animated math function graph plot

### https://satisui.xyz/ — 41 components
_Full catalog enumerated: sitemap.xml lists exactly 41 component doc pages (categories: wheels 3, text-animations 7, components 11, carousels 5, cards 9, buttons 5, animations-and-transitions 1); every page's official meta description was fetched, and the crawl map found no component pages beyond the sitemap. Pro components on pro.satisui.xyz (separate paid site) were not enumerated._

- **Radial Scroll Gallery** `scroll` — Scroll-driven interaction rotating items along a large, partially hidden circle
- **Pinned Card Fan** `scroll` — Pins to viewport and fans children out in an arc as the user scrolls; vertical-stack fallback on mobile/reduced motion
- **Circular Gallery** `3d` — 3D circular gallery rotating elements around a central Y-axis based on vertical scroll position
- **Typography Reveal** `text` — Reveals text unit-by-unit on scroll into view with a variety of GSAP animation effects
- **Text Sparkles** `text` — Animated sparkle effect over children, with DOM and high-performance Canvas render modes
- **Reveal Text** `text` — Vertical hover reveal: detects mouse entry from top or bottom and slides a colored text shadow in from that direction
- **Pulsing Text** `text` — Subtle rhythmic pulse animation applied to children
- **Fluid Text** `text` — Distorts and scales characters by mouse proximity while normalizing total width to stay constant
- **Flipping Word Swap** `text` — Flips a word character-by-character to a second word on hover
- **Color Cycle Text** `text` — Animates a text span by cycling through an array of colors
- **Urgency Countdown Timer** `other` — Versatile countdown timer showing time remaining until a target date
- **Timeline** `other` — Responsive animated vertical timeline for events or steps in chronological order
- **Text Highlighter Sharer** `other` — Shows a popover with sharing options when a user selects a text snippet
- **Sliding Capsule Nav** `nav` — Navigation with fluid layout animations, active-state detection, and z-indexed capsule layering
- **Rating** `other` — Animated rating component with star, emoji, text, and gradient-fill variants
- **Proximity Image Row** `cursor` — macOS Dock-style image row that magnifies near the cursor (CSS hover or fluid JS cursor-tracking modes)
- **Pricing Card** `card` — Responsive single pricing-tier card that adapts to the selected billing interval
- **Gliding Card** `card` — Floating card that glides smoothly between list items via shared hover state
- **Expanding Grids** `other` — Responsive grid animating expansion of a row and column when a cell is hovered or focused
- **Bento** `other` — CSS Grid container for building bento-box layouts
- **Animated Metric** `chart` — Numerical metric that animates in and counts up to a target value on scroll into view
- **Thumb Progress Carousel** `carousel` — Auto-playing carousel with a thumbnail strip visualizing remaining time for the current slide
- **Story Carousel** `carousel` — Instagram-story style carousel with auto-advance, segmented progress bars, and pause-on-hover
- **Fanned Card Stack** `carousel` — Fanned-out card stack; drag the top card to swipe it to the bottom
- **Drifting Marquee** `carousel` — Horizontal marquee that accelerates and skews with vertical scroll velocity, with optional touch/drag
- **3D Drifting Marquee** `3d` — Physics-based 3D infinite scroll with drag, scroll-velocity acceleration, and cinematic entry animations
- **Tilt Card** `card` — 3D lift-and-tilt on hover with a mouse-tracking aurora glow
- **Testimonial Card** `card` — Layered, visually engaging card for displaying testimonials
- **Proximity Cards** `card` — Container applying a 3D proximity hover effect to children, creating a wave as the mouse moves
- **Note Card** `card` — Styled container for tips, warnings, or highlighted info with a distinct visual accent
- **Focus Card** `card` — Orchestrates shared hover state for image, overlay, and content animations
- **Flip Card** `card` — 3D flip card with automatic, gesture-based, and programmatic flipping
- **CTA Card** `card` — Responsive call-to-action card with image, text, and email subscription form
- **Blog Card** `card` — Flexible blog-post summary card that shows/hides elements based on props
- **Animated Profile Card** `card` — Circular reveal animation on hover for profile content
- **The Eclipse** `button` — Magnetic-pull button with an eclipse color-inversion animation on hover
- **Prime Button** `button` — Async-action button managing loading/success/error states with layout animations and auto-reset
- **Morph Button** `button` — Fluid width morph between text state and a circular loading state
- **Micro Expander** `button` — Circular icon button that expands into a text pill on hover
- **Curtain Button** `button` — Curtain hover effect: background slides up and text color inverts, with loading-state handling
- **Enter** `scroll` — Wrapper that animates its children into the viewport when they scroll into view

### https://chanhdai.com/components — 35 components
_Full catalog enumerated. Index page explicitly lists "35 components"; all 35 detail pages were fetched (tavily map + 3 extract calls) and each description above is the component's own tagline. Site cross-sale pages found via map (/components/showcase, external kibo-ui/shadcn links) are not components and were excluded. Scroll Fade Effect is marked deprecated on its page; Shimmering Text recommends the CSS shadcn shimmer utility as a faster alternative._

- **Apple Hello Effect** `text` — SVG handwriting animation inspired by Apple's "Hello" screen (English/Hindi/Spanish/Vietnamese variants)
- **Brand Assets Menu** `nav` — Right-click/press-and-hold context menu for copying brand SVGs and opening asset links
- **Chevrons Up Down Icon** `other` — Animated chevrons icon that morphs between up and down directions
- **Code Block Command** `other` — Install-command code block with pnpm/yarn/npm/bun switcher and animated copy button
- **Consent Manager** `other` — Cookie/tracking consent banner for Next.js built on c15t, themed to shadcn/ui
- **Copy Button** `button` — Copy-to-clipboard button with animated icon states plus haptic and audio feedback
- **Dot Grid Spotlight** `background` — Interactive dot grid background with a cursor-tracking spotlight effect
- **Elastic Slider** `other` — Slider with elastic rubber-band stretch past bounds and magnetic snap to steps
- **Fluid Gradient Text** `text` — Text with a fluid gradient that shifts with horizontal pointer movement
- **GitHub Contributions** `chart` — Year-long GitHub contribution calendar heatmap with tooltips, themes, and profile link
- **GitHub Stars** `other` — GitHub repo star-count badge with formatted numbers and full-count tooltip
- **Glow Card Grid** `card` — Card grid with glowing border and background hover effects
- **Haptic Feedback** `other` — Trigger haptic vibration feedback on mobile devices (Vibration API + iOS trick)
- **Icon Swap** `other` — Animated icon swap with scale, blur, and fade transitions
- **Line Nav** `nav` — Vertical navigation with a line marker that expands on hover and active state
- **Logos Carousel** `other` — Logo wall that cycles logos column by column in a staggered wave with blur/slide transitions
- **Middle Truncation** `text` — Pixel-accurate middle text truncation preserving start and end, responsive to resize
- **Mobius Loop Icon** `other` — Animated Mobius loop icon that morphs between circles and an infinity shape
- **React Wheel Picker** `other` — iOS-like wheel picker with inertia scrolling and infinite loop support
- **Scroll Fade Effect** `scroll` — Fades content edges as you scroll, vertical and horizontal (deprecated in favor of shadcn scroll-fade utils)
- **Share Menu** `nav` — Share menu to copy a link or post to X, LinkedIn, and the native share sheet
- **Shimmering Text** `text` — Smooth light-sweeping shimmer animation across text
- **Slide to Unlock** `button` — Draggable slider recreating the classic iPhone slide-to-unlock gesture with shimmer text
- **Spinning Circular Text** `text` — Text arranged in a circle with continuous spinning animation
- **Spotlight Logo** `cursor` — SVG logo with cursor-tracking radial-gradient highlight and springy tactile press
- **Testimonial** `card` — Testimonial card with quote, author info, avatar, and verified badge
- **Testimonial 2** `card` — Testimonial quote with author attribution and source link
- **Testimonial Spotlight** `card` — Testimonial card with cursor spotlight hover effect
- **Testimonials Marquee** `card` — Continuous scrolling testimonial marquee with fade edges, single or multi-row
- **Text Flip** `text` — Text that cycles through items with a smooth flip transition
- **Theme Switcher** `button` — Toggle between system, light, and dark themes for Next.js apps
- **Theme Toggle Effect** `background` — View Transitions API light/dark switch animations (triangle, circle, polygon, blur variants)
- **TOC Minimap** `nav` — Compact hoverable table-of-contents minimap with active-section tracking and smooth scroll
- **Twemoji** `other` — Renders Unicode emoji as consistent Twemoji SVG images inline with text
- **Work Experience** `card` — Resume-style work experience list with company logos, roles, and durations

### https://vibecodecomponents.com/navigation-components — 235 components
_Full catalog enumerated: site mapped via tavily_map (27 internal URLs), then all 17 catalog category pages fetched (navigation, css-animations, text-effects, hero, micro-interactions, ui-polish, dashboard-kit, cta-components, saas-kit, onboarding-kit, storytelling, mobile-optimization, content-components, ai-kit, form-components, media-kit, ecommerce-kit, highlevel) plus the /premium index which cross-confirms 82 premium components across 9 kits. Cross-listed duplicates merged (e.g. Skeleton Loading, Typing Indicator, Countdown Hero, Interactive Timeline, FAB appear on multiple pages, counted once). Non-catalog pages (/auth login, /favorites, /privacy, /terms, /products demo breadcrumb pages) contain no components. Descriptions come from server-rendered page text; interactive premium demos are gated but names/descriptions are public. Category slugs restricted to the requested set, so form/media/ecommerce/hero items fall under "other"._

- **Top Navigation Bar** `nav` — Fixed header with logo, centered links, action icons and notification badge
- **Hamburger Menu** `nav` — Mobile toggle animating hamburger to X with staggered dropdown items
- **Slide-In Menu** `nav` — Off-canvas sidebar sliding from left over blurred backdrop with profile and menu items
- **Mega Menu** `nav` — Multi-column hover dropdown with categorized items, icons and promo banner
- **Breadcrumbs** `nav` — Path navigation in four styles: chevron, slash, pill, contained
- **Floating Action Button** `button` — Circular fixed button expanding into staggered action options with icon rotation
- **Back to Top Button** `button` — Scroll-triggered circular arrow button that smooth-scrolls to top
- **Dock Navigation (Premium)** `nav` — macOS-style horizontal icon dock with hover magnification
- **Animated Tab Bar (Premium)** `nav` — Tabs with sliding indicator animations between sections
- **Advanced Pagination (Premium)** `nav` — Numbered pages with ellipsis, page-jump input and result info
- **Responsive Breadcrumb (Premium)** `nav` — Deep path that collapses middle items on narrow screens with expand toggle
- **Pulsing Circle** `other` — Central circle with expanding animated ping rings
- **Bouncing Dots** `other` — Three staggered bouncing dots for loading states
- **Spinning Loader** `other` — Classic circular spinner with partial colored border
- **Wave Animation** `other` — Eight vertical bars forming an audio-visualizer wave
- **Skeleton Loading** `other` — Shimmer gradient moving across placeholder content blocks
- **Typing Indicator** `other` — Three sequential pulsing dots in a chat bubble
- **Floating Element** `other` — Gently hovering element with inversely scaling shadow
- **Confetti Burst** `other` — Colored particles flying outward from center on click
- **Wiggle Attention** `other` — Subtle back-and-forth rotation for notification badges
- **Slide-In Banner** `other` — Notification bar sliding down with gradient background
- **Countdown Timer (circular)** `other` — SVG circular progress ring showing remaining seconds
- **Progress Bar Fill** `other` — Horizontal gradient bar with shimmer overlay
- **Card Flip** `card` — 3D rotating card revealing back content on click
- **Accordion Animation** `other` — Expandable sections with smooth height/opacity transitions
- **Ripple Touch** `button` — Material-style expanding ripple from click coordinates
- **Neon Glow** `text` — Pulsing text with layered shadow glow
- **Rotating Gradient Border** `other` — Avatar/card framed by rotating conic-gradient ring
- **Orbiting Icons** `other` — Feature icons rotating on a circular path around a hub
- **Number Counter** `text` — Statistics sliding up into view with staggered timing
- **Ambient Gradient** `background` — Slow-moving multi-stop gradient background shift
- **Rotating Text Loader** `other` — Spinning icon with fading rotating text phrases
- **Morphing Loaders (Premium)** `other` — Shape-shifting blob with continuous border-radius morphing (incl. DNA helix and 3D cube variants)
- **Circular Progress (Premium)** `chart` — SVG circle with animated stroke-dashoffset, gradient and segmented variants
- **Glow Effects (Premium)** `background` — Colored orbs with staggered pulsing box-shadow glow
- **Gradient Backgrounds (Premium)** `background` — Aurora, mesh, waves and floating-orb animated backgrounds
- **Animated Borders (Premium)** `other` — Chase, spin and glow effects around element edges
- **Typewriter Effect** `text` — Text types character-by-character with blinking cursor
- **Word Rotator** `text` — Words cycle with fade transitions
- **Text Scramble** `text` — Letters scramble through random characters before revealing
- **Gradient Text Animation** `text` — Moving gradient flows across text
- **Character Stagger** `text` — Letters animate in sequentially with staggered delays
- **Highlight Marker** `text` — Animated highlight bar grows behind text
- **Counting Numbers** `text` — Statistics animate from zero to target value
- **Blinking Cursor** `text` — Standalone vertical caret with opacity blink
- **Wave Text** `text` — Letters move up and down in a sine wave
- **Bounce Text** `text` — Letters bounce in from above with spring physics
- **Flip Text** `text` — Words flip vertically like airport departure boards
- **Fade Text Swap** `text` — Words fade out/in cycling through a list
- **Text Stroke Animation** `text` — SVG text with animated stroke drawing
- **Blur to Sharp** `text` — Text transitions from blurred to crisp
- **Floating Letters** `text` — Letters gently float up and down independently
- **Word-by-Word Reveal** `text` — Words appear sequentially with fade and slide
- **Slide Text Swap** `text` — Current word slides up as the next enters below
- **Text Mask Reveal** `text` — Sliding mask uncovers text cinematically
- **Split Text Reveal (Premium)** `text` — Words split and animate independently on scroll
- **Text Morphing (Premium)** `text` — One word transforms into another via blur transition
- **Glitch Text (Premium)** `text` — Cyberpunk RGB color-split jitter effect
- **3D Text Flip (Premium)** `text` — Letters flip on axis with perspective depth
- **Kinetic Typography (Premium)** `text` — Words bounce and float with physics simulation
- **Text Scroll Reveal (Premium)** `text` — Clip-path reveals text as user scrolls
- **Rainbow Wave Text (Premium)** `text` — Hue rotation cascades through letters
- **Liquid Text Fill (Premium)** `text` — SVG text fills with wave animation from bottom
- **Neon Glow Text (Premium)** `text` — Pulsing neon sign with flicker
- **Matrix Rain (Premium)** `text` — Green falling character columns like The Matrix
- **Magnetic Text (Premium)** `cursor` — Letters attract/repel based on cursor position
- **Text Explosion (Premium)** `text` — Letters scatter on hover then reassemble
- **Elastic Text (Premium)** `text` — Letters stretch and bounce with spring physics
- **Wobble/Jelly Text (Premium)** `text` — Text wobbles continuously with skew transforms
- **Hover Reveal (Premium)** `text` — Hidden text revealed beneath visible text on hover
- **Text Fill on Scroll (Premium)** `scroll` — Text color fills left-to-right as you scroll
- **3D Text Rotation (Premium)** `3d` — Text rotates continuously in 3D space
- **Parallax Text (Premium)** `scroll` — Text layers move at different scroll speeds
- **Slot Machine Text (Premium)** `text` — Characters spin like casino reels landing on words
- **Mechanical Odometer (Premium)** `text` — Digits flip like a physical counter
- **Terminal Command (Premium)** `text` — Command types with syntax highlighting and output
- **Animated Underline Draw (Premium)** `text` — Line draws under text with scaleX animation
- **Smoke Dissolve (Premium)** `text` — Text fades and blurs upward like vapor
- **Hero Section** `other` — Gradient hero with badge, headline, dual CTAs and trust logos
- **Video Background Hero** `other` — Full-bleed video hero with overlay and pulsing live indicator
- **Split Hero** `other` — Two-column hero pairing decorative graphics with content and features
- **Carousel Hero** `other` — Auto-rotating hero slides with dots, arrows and progress bar
- **Announcement Bar** `other` — Dismissible top banners in promo/alert/urgency/update variants
- **Animated Text Hero (Premium)** `other` — Hero with rotating/morphing headline text
- **Countdown Hero (Premium)** `other` — Launch countdown hero with email capture
- **Parallax Hero (Premium)** `other` — Mouse-tracking depth across layered hero elements
- **Button Ripple Effect** `button` — Material click ripple expanding from click point
- **Hover Animation Cards** `card` — Multi-layer hover cards with gradient glows and sliding titles
- **Scroll-Reveal Animations** `scroll` — Elements fade/slide in with staggered timing on viewport entry
- **Animated Counters** `text` — Numbers count up with easing when scrolled into view
- **Smooth Scroll Navigation** `nav` — Nav bar with vertically sliding content panels between sections
- **Magnetic Button (Premium)** `cursor` — Button that follows the cursor when hovering nearby
- **Parallax Tilt Card (Premium)** `3d` — Card tilts in 3D perspective with glare following cursor
- **Morphing Button (Premium)** `button` — Button morphs through idle, loading and success states
- **Particle Trail (Premium)** `cursor` — Cursor leaves glowing colorful particle trail
- **Elastic Motion (Premium)** `other` — Physics-based bouncy tab selector with sliding background
- **Custom Cursors** `cursor` — CSS cursor styles from built-ins to custom images and colors
- **Styled Scrollbars** `scroll` — WebKit scrollbar customization with themed color schemes
- **Selection Styles** `other` — Branded text-highlight colors via ::selection
- **Cursor Follower** `cursor` — Glowing element smoothly trailing the mouse
- **Glassmorphism Card** `card` — Frosted-glass card with backdrop blur and transparency
- **Emoji Picker** `other` — Emoji selector with categories, search, skin tones and keyboard nav
- **KPI Stat Cards** `chart` — Metric cards with colored icons, large values and trend indicators
- **Mini Charts** `chart` — Compact sparkline and bar widgets with percentage changes
- **Filter Sidebar** `nav` — Collapsible filter panel with checkboxes and price ranges
- **Profile Dropdown** `nav` — Avatar-triggered user menu with name and options
- **Dashboard Search** `other` — Search input with keyboard-shortcut hint and suggestions
- **Export Dropdown** `other` — Button menu offering multiple file-format downloads
- **Settings Panel** `other` — Card of toggle switches for preferences like dark mode
- **CTA Blocks** `other` — Single, dual and inline call-to-action layouts with badges
- **Sticky CTAs** `other` — Fixed bottom bar with pricing and buy button over blur backdrop
- **Countdown Timer (flip)** `other` — Flip-clock urgency timer showing days/hours/minutes/seconds
- **Exit Intent Popup** `other` — Full-screen exit-triggered modal with email capture
- **Floating Lead Bar** `other` — Collapsible newsletter signup bar fixed to viewport bottom
- **Scroll Progress** `scroll` — Gradient bar tracking reading position
- **Animated CTA Buttons (Premium)** `button` — Button variants with glow, ripple, slide, pulse and bounce
- **Split Test CTA Variants (Premium)** `other` — A/B comparison of three CTA versions with metrics
- **Interactive Pricing Slider (Premium)** `other` — Dynamic pricing calculator driven by team-size slider
- **Tabs Navigation** `nav` — Horizontal tab switcher with active state and keyboard nav
- **Tooltips** `other` — Hover-triggered info boxes with positional variants
- **Data Table with Sorting (Premium)** `other` — Sortable, searchable table with column headers
- **Notification Center (Premium)** `other` — Dropdown notifications with unread badge and filtering
- **Activity Feed (Premium)** `other` — Vertical timeline of timestamped actions with category icons
- **Empty States (Premium)** `other` — Placeholder UIs for empty inbox / missing files scenarios
- **Stats Cards Dashboard (Premium)** `chart` — Metric grid with percentage changes and icons
- **User Avatar Group (Premium)** `other` — Overlapping stacked avatars with display limit
- **Sidebar Navigation (Premium)** `nav` — Collapsible app sidebar with logo, items and status dots
- **Command Bar / Search (Premium)** `nav` — Keyboard command palette with searchable actions
- **Pricing Table (Premium)** `other` — Tiered pricing plan cards (listed in SaaS kit on premium page)
- **Feature Comparison (Premium)** `other` — Plan feature comparison grid (SaaS kit)
- **Testimonial Wall (Premium)** `card` — Grid wall of customer testimonials (SaaS kit)
- **Welcome Modal** `other` — First-run greeting modal with gradient header and centered icon
- **Tooltip Tour** `other` — Sequential feature walkthrough across app sections
- **Progress Checklist** `other` — Setup completion tracker with progress bar
- **Feature Spotlight** `other` — Card with pulsing ring highlight and New Feature badge
- **Setup Wizard** `other` — Multi-step configuration flow with step indicator tabs
- **Celebration Animation** `other` — Confetti particles and achievement display on success
- **Milestone Timeline** `other` — Vertical gradient timeline with circular milestone icons
- **Before/After Slider** `other` — Draggable clipPath comparison slider
- **Journey Map** `other` — Five-stage horizontal customer journey with emotion labels
- **Testimonial Scroller** `card` — Auto-rotating testimonial carousel with ratings and dots
- **Video Testimonials** `other` — Video gallery with main player and thumbnail strip
- **Case Study Cards** `card` — Stacked cards with company, industry, metrics and hover arrows
- **Quote Block** `other` — Stylized blockquote with quote icon, avatar and gradient accent
- **Brand Story Section** `other` — About section with narrative, values icons and stats grid
- **Interactive Timeline (Premium)** `other` — Expandable vertical timeline with detailed content panels
- **Stats Counter Section (Premium)** `chart` — Animated number counters activating on scroll
- **Team Section (Premium)** `card` — Team member cards with gradient avatars and social links
- **Process Steps (Premium)** `other` — Numbered horizontal flow diagram with icons
- **Awards & Recognition (Premium)** `card` — Grid of award badges, logos and certifications
- **PWA Install Prompt** `other` — Device-aware add-to-home-screen guide
- **Accordion Sections** `other` — Collapsible FAQ cards with rotating chevrons
- **Card-Based Layout** `card` — Responsive 2x2 feature grid with icons and hover states
- **Sticky Bottom Navigation (Premium)** `nav` — Fixed thumb-friendly 5-icon bottom dock
- **Sticky Bottom CTA (Premium)** `other` — Persistent CTA bar visible while scrolling
- **Mobile FAB with Gestures (Premium)** `button` — FAB with edge-swipe triggers and haptic feedback
- **Expandable Content (Premium)** `other` — Text truncation with gradient fade and Show More toggle
- **Swipeable Carousel (Premium)** `other` — Touch carousel with 80% card visibility swipe cue
- **Quick-Action Toolbar (Premium)** `other` — Multi-step form bar with Back/Save/Next controls
- **Mobile Stepper (Premium)** `other` — Horizontal step progress indicator for flows
- **Bottom Sheet/Drawer (Premium)** `other` — Uber/Apple-Maps-style slide-up panel
- **Quick Filter Chips (Premium)** `other` — Horizontally scrolling filter chip row
- **Swipe-to-Delete List (Premium)** `other` — List items with swipe gesture actions
- **Tinder-Style Swipe Cards (Premium)** `card` — Stackable swipe-left/right selection cards
- **Pull-to-Refresh List (Premium)** `scroll` — Native pull gesture with resistance triggering refresh
- **Path Selection Carousel (Premium)** `other` — Single centered card carousel for onboarding choices
- **Timeline / Roadmap** `other` — Vertical roadmap with completed/current/upcoming states
- **Step-by-Step Progress** `other` — Horizontal numbered step indicator with connecting lines
- **Card Grid** `card` — 2x2 feature card grid with icon containers and hover
- **Icon List** `other` — Feature list with circular icons and one highlighted item
- **Comparison Table** `other` — Three-tier feature comparison with row hover
- **Service Matrix** `other` — Package grid with checkmark/minus inclusion indicators
- **Image Gallery** `other` — 3x2 thumbnail grid with lightbox and keyboard controls
- **Masonry Grid** `card` — Pinterest-style varied-height 3-column grid with hover scale
- **Interactive Carousel Photo (Premium)** `other` — Centered photo-card carousel with category selection
- **Video Gallery (Premium)** `other` — 2x3 video thumbnail grid with modal player
- **Advanced Data Table (Premium)** `other` — Sortable table with search, role filter, export and pagination
- **Chat Interface (Premium)** `other` — Full AI conversation UI with user/AI message bubbles
- **Streaming Text Animation (Premium)** `text` — Character-by-character ChatGPT-style reveal
- **Code Block with Syntax (Premium)** `other` — Language-tabbed code display with highlighting
- **AI Feedback Controls (Premium)** `button` — Thumbs up/down with copy and report options
- **Prompt Input Field (Premium)** `other` — Multi-variant AI query input with suggestions
- **Model Selector (Premium)** `other` — Grid/dropdown of AI models with descriptions
- **Response Card (Premium)** `card` — Collapsible AI response container with token/cost info
- **Multi-Step Form** `other` — Horizontal step-circle progression with navigation
- **Floating Label Inputs** `other` — Material inputs with labels floating on focus
- **Password Strength Indicator** `other` — Real-time strength meter with requirements checklist
- **Password Visibility Toggle** `other` — Password input with eye icon show/hide
- **Forgot Password Flow** `other` — Email validation form with success state
- **Search with Suggestions** `other` — Search bar with recent and trending suggestions
- **Typeform-Style Form** `other` — One-question-per-screen card flow with progress
- **Input Masks** `other` — Auto-formatting phone, date, card and SSN inputs
- **Validated Inputs** `other` — Real-time validation with colored borders and errors
- **Date Pickers** `other` — Single and range date selection with calendar popover
- **File Upload Dropzone** `other` — Drag-and-drop zone with progress bars and type icons
- **Multi-Select** `other` — Searchable select with removable tags and checkboxes
- **Range Sliders** `other` — Single and dual-handle sliders with value displays
- **Star Rating** `other` — Interactive 5-star selector with hover preview
- **Basic Radio Group** `other` — Vertical radio options with default selection
- **Radio Cards (Premium)** `card` — Selectable card-style radios with icons
- **Horizontal Radio** `other` — Inline row of radio buttons
- **Radio with Description** `other` — Radios with titles and helper text
- **Segmented Control** `other` — iOS-style pill switcher with smooth transitions
- **Visual Icon Radio** `other` — Icon-based card selection for themes
- **Voice Typing** `other` — Speech-to-text input via Web Speech API
- **Enter Key Form** `other` — Multi-field form navigated by Enter key
- **OTP / PIN Input (Premium)** `other` — 6-digit verification code input with auto-focus
- **Signature Pad (Premium)** `other` — Canvas signature capture with touch support
- **Address Autocomplete (Premium)** `other` — Smart address input with type-ahead suggestions
- **Conditional Form Fields (Premium)** `other` — Fields that show/hide based on selection
- **Credit Card Form (Premium)** `other` — Payment form with live card preview and validation
- **Form Review Step (Premium)** `other` — Summary step with collapsible verification sections
- **Quantity Stepper (Premium)** `other` — Increment/decrement input for carts
- **Rating with Feedback (Premium)** `other` — Star rating with contextual feedback prompts
- **Image Lightbox Modal** `other` — Full-screen image viewer with zoom and navigation
- **Masonry Photo Grid** `other` — Pinterest-style variable-height photo layout
- **Video Player with Custom Controls** `other` — YouTube-style player with branded controls
- **Audio Player with Waveform** `other` — Spotify-style player with visual waveform
- **Drag-and-Drop File Upload** `other` — Dropzone with visual feedback and progress
- **Image Crop/Edit Modal** `other` — In-app crop, rotate and adjust editor
- **Multi-File Upload Progress** `other` — Batch upload with per-file and overall progress
- **Thumbnail Strip Navigator** `nav` — Horizontal scrollable gallery nav with active indicator
- **Product Card** `card` — Product image, price, wishlist heart and quick-add
- **Shopping Cart Drawer** `other` — Slide-out cart with items and checkout totals
- **Product Gallery** `other` — Main product image with thumbnails and zoom
- **Variant Selector** `other` — Color swatches and size options with stock states
- **Order Summary** `other` — Checkout summary with promo code field
- **Wishlist Button** `button` — Animated heart toggle with saved-items list
- **Recently Viewed** `other` — Horizontal carousel of browsed products
- **Order Confirmation** `other` — Success page with order number and delivery tracking
- **Invoice Preview** `other` — Invoice display with line items, totals and download
- **Subscription Selector** `other` — Plan cards with monthly/yearly billing toggle
- **Payment Receipt** `other` — Transaction success confirmation with details
- **Billing History Table** `other` — Past invoices with status and download links
- **SMS Reminder System (HighLevel)** `other` — Scheduling UI with phone, time, timezone and toggle plus API/cron integration docs

### https://glass3d.dev/ — 6 components
_Full coverage, but glass3d.dev is not a multi-component library: it is a single-page CSS glassmorphism generator (one .glass3d effect plus its generator controls). Verified via tavily_map (no crawlable links), sitemap.xml 404, full HTML of index.html and the fetched generator.html partial, and main.js — the SPA loads only generator.html; there is no components index, docs section, or any other catalog page. The 6 entries above are the complete inventory: 1 shippable effect and 5 generator UI features._

- **glass3d effect (.glass3d CSS)** `card` — Layered glassmorphism surface: backdrop-filter blur/brightness/saturate, tinted HSL overlay, repeating noise texture, multi-step drop shadows plus inset edge highlights for a 3D glass look on any container.
- **Backdrop filter controls** `other` — Generator sliders/toggles for blur, brightness, and saturate values of the glass backdrop-filter.
- **Glass tint color picker** `other` — HSL (h/s/l/a) slider picker with live swatch that sets the glass overlay color.
- **Noise texture selector** `background` — Five selectable noise overlays (light, gray, dark, coarse, topology) tiled over the glass surface for frosted grain.
- **Preview background tabs** `other` — Dark image, Light image, and Video preview backgrounds to test the glass effect against different content.
- **Copy-to-clipboard CSS output** `other` — Live-updating CSS code block of the generated .glass3d rules with a copy button.

### https://www.originkit.dev/components/svgparticles — 82 components
_Full catalog enumerated: 82 components, matching the site's own sidebar category counts (Text 21, Button 2, Border 1, Image 8, Image Gallery 16, Cursor 5, Elements 13, Animations 5, Background 11 = 82). Discovered via tavily_map (65 URLs) plus sidebar HTML parsing of a component page (17 additional slugs the crawler missed, e.g. pixelcard, scrambletext, spinimage). Every component's own page was fetched for its name and tagline (tavily_extract for ~33, curl title/meta for the rest). Categories are my mapping to the requested taxonomy; the site's native categories differ slightly (e.g. Image, Image Gallery, Border, Elements mapped mostly to "other")._

- **Axis Cursor** `cursor` — Precision crosshair cursor with live X/Y coordinate readout and optional center dot
- **Black Hole** `background` — 3D black hole with orbiting particles, gravity inflow, and glowing event horizon in canvas
- **Blinking Squares** `background` — Grid of independently twinkling squares with directional density fade and cursor halo
- **Blur Carousel** `other` — Carousel with edge blur creeping in from hovered arrow and 3D press-tilt slide advance
- **Box Carousel** `3d` — Images/videos rotating across the four faces of a 3D cube via autoplay, drag, or arrows
- **Button Carousel** `button` — Ring of orbiting buttons that swings a portrait and label into view on each tap
- **Character Waves** `background` — Flowing ASCII character field driven by layered noise waves that ripple from the cursor
- **Chromatic Waves** `background` — Rainbow Perlin-noise field rendered as a drifting halftone dot-matrix grid in WebGL
- **Click Effects** `cursor` — Six click-triggered effects (rings, burst, particles, crosshair, wavy, sniper) fired at click point
- **Coverflow Carousel** `other` — Active card expands to a landscape hero while others shrink to thin slats, infinite loop
- **Coverflow Gallery** `3d` — Smooth 3D coverflow carousel with perspective tilt, autoplay, and customizable titles
- **Cursor Image Gallery** `cursor` — Pointer becomes a directional arrow; hover and click to slide through images
- **Curved Marquee** `text` — Draggable text marquee flowing along a curved SVG path with speed and edge-fade controls
- **Direction Hover** `text` — Text swaps to an accent copy sliding in from the edge the cursor entered
- **Dot Image Reveal** `other` — Image as an interactive dot grid that opens around the cursor
- **Dot Matrix** `background` — Animated Perlin-noise dot grid background with gradient palette and optional glyph mode
- **Draggable Grid** `other` — Infinitely draggable image canvas with momentum and wheel navigation
- **Draggable Sticker** `3d` — Image dragged like a real sticker: WebGL peel curl, velocity tilt, sheen, and settle-back
- **Dust Text Reveal** `text` — Text assembles itself from a drifting cloud of glowing dust particles on hover or scroll
- **Dynamic Weight** `text` — Each letter morphs between two variable-font weights based on cursor proximity
- **Electric Border** `other` — Crackling animated noise-displaced electric outline with glow wrapping any frame
- **Emoji Burst** `button` — Button that fires a gravity-driven emoji particle burst on click with springy shake
- **Flicker Text** `text` — Text or image flicker/outline effects with word- and letter-level control, on enter or hover
- **Floating Icons** `background` — Emoji or images rising and drifting upward in staggered organic batches
- **Fluid Image Reveal** `other` — WebGL shader unveiling images through organic morphing blobs, scroll-triggered
- **Fluid Trail** `cursor` — GPU Navier-Stokes fluid simulation trail swirling and glowing behind the cursor
- **Glitch Text** `text` — Glitchy distorted text/image reveal with shake and RGB slice-tear, on hover or scroll
- **Glitter Wrap** `background` — Starfield of particles warping outward/inward from center with sparkle flashes and streaks
- **Globe** `3d` — Interactive 3D rotating globe with dot or solid landmass fill, geo markers, and grid lines
- **Gravity Gallery** `other` — Image-filled squares/circles dropped into a Matter.js physics world with drag tossing
- **Hover Image Reveal** `other` — Hover through named rows while a cursor-following image window switches to match
- **Image Fold** `3d` — Photo unrolls like a rolled-up scroll via a WebGL 3D shader, scroll-triggered or on load
- **Image Gallery** `other` — Images continuously stream flying outward or spiraling from the center
- **Image Ripple** `other` — Image revealed by a colorful particle ripple with 7 wave shapes, on load/scroll/click
- **Infinity Canvas** `other` — Infinitely zoomable and draggable image gallery with smooth momentum navigation
- **Inkbleed** `text` — Cursor-reactive gooey ink-bleed text with liquid blur and character merging
- **Juice Effect** `other` — Image silhouette filled with gooey rising/falling liquid particles that fuse into blobs
- **Kinetic Grid** `background` — Cursor-reactive dot grid background with animated mesh lines and optional trail
- **Line Ripple Background** `background` — Cursor-reactive flow field of noise-driven line streaks rippling across the background
- **Link Preview** `nav` — Floating page thumbnail revealed on link hover, leaning toward the cursor with springs
- **Liquid Distortion** `other` — Real-time WebGL fluid simulation adding cursor-driven ripples to any image
- **Magnetic Carousel** `other` — Row of image bars magnifying macOS-dock style as the cursor nears
- **Mesh Text Hover** `text` — Text warped across a WebGL mesh that drags and springs back under the cursor
- **Particle Sphere** `3d` — Interactive 3D sphere of thousands of glowing particles scattering from the cursor
- **Particle Tunnel** `background` — Particles flowing along radial spokes toward a central black void with perspective depth
- **Pixel Card** `card` — Card filled with a shimmering pixel grid growing in from center or any edge on hover
- **Pixel Drift** `text` — Interactive particle typography that assembles and disperses
- **Pixel Reveal** `other` — Grid of colored pixel squares dissolving directionally to reveal an image
- **Pixel Unfold** `scroll` — Image revealed tile by tile on scroll with adjustable pixel size and replay
- **Pixelate Image** `other` — Real-time SVG pixelation filter revealing/concealing an image by cursor distance
- **Prism Grid** `background` — Interactive 3D-tilted grid whose cells light up in random colors on hover
- **Proximity Orbit** `other` — Images arranged along a continuously rotating circular path
- **Pulse Lines** `background` — Animated background of sweeping colored lines, dots, or squares with palette control
- **Random Letter Swap** `text` — Each heading letter swaps vertically on hover, revealing a duplicate glyph in random order
- **Reactive Grid** `background` — Canvas particle grid that blooms around the cursor with smooth easing
- **Reactive Lines** `background` — Curved lines responding to cursor position in real time with density and fade controls
- **Rising Lines** `background` — Two layers of glowing particles (line sparks and soft blobs) rising or falling
- **Round Carousel** `3d` — Draggable 3D cylinder image gallery with auto-rotation, momentum spin, mirrored cards
- **Scramble Text** `text` — Cinematic glitch text reveals with interactive character-based hover scrambling
- **Scroll Text Reveal** `scroll` — Scroll-triggered text reveal with per-character/word/line masking, blur, and offset
- **Shiny Pill** `text` — Bright sheen sweeping across a line of text on an endless loop
- **Smoky Text** `text` — Text animated with realistic smoke diffusion and customizable reveal directions
- **Snow Fall** `background` — Drifting canvas snow with per-flake size, speed, opacity, and wind sway
- **Spin Image** `3d` — Images orbiting a tilted 3D ellipse with continuous spin and depth-based scaling
- **Spiral Images** `other` — Images flowing along an Archimedean spiral vortex from edge to center
- **Spotlight Text** `text` — Dimmed text block with a soft cursor-following spotlight revealing bright letters
- **Star Burst** `background` — Glowing streaks radiating from a movable focal point forming a twinkling starburst
- **Stardust** `background` — Drifting ambient particle background with tunable density, size, speed, and drift angle
- **Sticker Peel** `3d` — 3D image sticker peeling and curling away from the surface on hover and press
- **SVG Particle** `other` — Images/SVGs turned into interactive particle animations with hover and cursor repulsion
- **Swipe Stack** `card` — Fanned 3D stack of image cards that tilt, scale, and cycle to the back on swipe
- **Text Reveal Wall** `text` — Words animate out of a grid of randomly scrambling characters line by line
- **Text Vaporize** `text` — Words dissolve into particles and reassemble into the next word
- **Text Wave** `text` — Area filled with a rippling character grid and a smooth color-gradient sweep
- **Text Lift** `text` — Letters stacked into layered 3D extrusions that expand and lift on hover
- **Text Morph** `text` — Word list cycling with a gooey blur-and-scale morph melting each word into the next
- **Text Path** `text` — Repeating text scrolling seamlessly along a procedural sine-wave path
- **Thunder Strike** `background` — Customizable WebGL lightning shader with color, speed, intensity, and angle controls
- **Type Writer** `text` — Rotating phrases typed out character by character with a blinking cursor
- **User Cursor** `cursor` — Native pointer replaced with a spring-tracked arrow and trailing name pill
- **Wave Arcs** `background` — Cursor-reactive canvas background of glowing, sweeping arc lines
- **Weight Hover** `text` — Variable-font weight morph per letter, staggered with a spring ease on hover

### https://text-effects.colorion.co/ — 66 components
_Full catalog enumerated: the site is a single-page library of exactly 66 pure-CSS text effects (no sub-pages; per-effect CSS/prompt served via on-page modals). All 66 names and effect types extracted from the page's own markup (data-name/data-type attributes). The site ships no per-effect prose, so one-line descriptions are inferred from each card's effect-type keyword and demo label; all components are category "text" by design._

- **Borealis** `text` — Aurora-style animated gradient sweeping through the letters
- **Glitchcore** `text` — RGB-split digital glitch jitter on the text
- **Teletype** `text` — Typewriter effect typing characters out sequentially
- **Neon-Haus** `text` — Glowing neon-sign text with flicker glow
- **Aqua-Fill** `text` — Liquid fill rising and sloshing inside the letterforms
- **Chromia** `text` — Polished chrome/metallic reflective text finish
- **Lens-Drift** `text` — Per-letter focus drift, letters blur in and out like a lens
- **Tidal-Type** `text` — Letters undulate in a wave motion
- **Bisect** `text` — Text sliced in half with offset sliding halves
- **Cipher** `text` — Decoder scramble, random glyphs resolve into the word
- **Redactor** `text` — Scanner bar sweeps across, revealing/redacting text
- **Emberglow** `text` — Smoldering ember glow burning through the letters
- **Echo-Verse** `text` — Repeating echo copies trailing behind the text
- **Deep-Type** `text` — Extruded stacked-shadow depth, faux-3D block text
- **Wireframe** `text` — Outline/contour-only stroked letterforms
- **Prisma** `text` — Per-letter rainbow spectrum coloring animation
- **Jitterbug** `text` — Nervous rapid jitter shake on each letter
- **Anaglyph-3D** `text` — Red/cyan anaglyph offset for 3D-glasses depth
- **Split-Flap** `text` — Airport split-flap board letters flipping into place
- **Phosphor** `text` — CRT phosphor terminal look with scanlines and glow
- **Pop-Riot** `text` — Comic-style letters popping/bouncing in with punch
- **Limelight** `text` — Moving spotlight illuminating the text from darkness
- **Rubber-Band** `text` — Elastic squash-and-stretch boing on letters
- **Still-Water** `text` — Mirrored water reflection beneath the text
- **Ransom-Note** `text` — Mismatched cut-out ransom-note letter styling
- **Meltdown** `text` — Letters dripping and melting downward
- **Cardio** `text` — Heartbeat pulse scaling of the text
- **Hi-Liter** `text` — Marker highlight swipe drawn across the words
- **Sundial** `text` — Rotating cast shadow like sunlight moving over a sundial
- **Negativ** `text` — Inverted negative-space color-flip treatment
- **Holograph** `text` — Shimmering hologram sheen with iridescent flicker
- **Gold-Foil** `text` — Luxe gold-foil metallic shine on the letters
- **Pixel-Sort** `text` — Pixelated/pixel-sorted glitch texture on text
- **Starlight** `text` — Twinkling stardust sparkle over the letters
- **Blueprint** `text` — Draft blueprint style, technical sketch lines
- **Vapor-Trail** `text` — Vaporwave-style trailing haze behind the text
- **Kinetic-Type** `text` — Kinetic typography, letters animate with staggered motion
- **Blackout** `text` — Classified-document blackout bars over the text
- **Magnetic** `text` — Letters attract/snap together as if magnetized
- **Luma-Mesh** `text` — Luminous mesh/gradient texture through the letters
- **Iridescent** `text` — Shifting iridescent oil-slick color sheen
- **Glass-Type** `text` — Frosted glass / glassmorphism translucent text
- **Datastream** `text` — Streaming data characters flowing through the text
- **Orbitals** `text` — Letters orbiting on circular paths
- **Prism-Cut** `text` — Faceted prism-cut refraction slicing the letters
- **Soft-Blur** `text` — Soft blur breathing in and out of focus
- **Laser-Cut** `text` — Laser beam tracing/cutting the letter outlines
- **Microchip** `text` — Circuit-board traces styled into the text
- **Heatmap** `text` — Thermal-imaging heatmap gradient across the letters
- **Parallax** `text` — Layered letters shifting at different depths (parallax)
- **Ink-Trap** `text` — Ink-trap typography with ink bleed detailing
- **Topographic** `text` — Topographic contour-map lines filling the letters
- **Signal-Noise** `text` — Static/noise interference distorting the signal text
- **Portal** `text` — Text emerging through a portal-style reveal
- **Tilt-Shift** `text` — Tilt-shift selective focus band across the text
- **Duotone** `text` — Two-tone split color treatment on the letters
- **Glyph-Rain** `text` — Matrix-style glyphs raining around/into the word
- **Zoetrope** `text` — Rotating zoetrope-style spinning letters
- **Dot-Matrix** `text` — Dot-matrix printer dot-grid rendering of text
- **Pendulum** `text` — Letters swinging like pendulums
- **Smoke-Signal** `text` — Letters dissolving into drifting smoke
- **Eclipse** `text` — Eclipse-style shadow disc passing over glowing text
- **Barcode** `text` — Barcode stripe styling built into the letters
- **Frostbite** `text` — Frozen frost/ice crystal texture creeping over text
- **Moiré** `text` — Moiré interference-pattern shimmer through the letters
- **Rubber-Stamp** `text` — Rubber-stamp slam-down imprint (APPROVED style)

### https://kinetics.colorion.co/ — 117 components
_Full catalog enumerated: all 117 effects. Site is a single-page gallery (no per-component subpages); tavily_map found no internal catalog URLs, so the complete list was extracted from the site's open-source repo (github.com/ckissi/kinetics, src/content/body.html), whose card count (117, sections Interaction & Input 39 / Feedback & State 39 / Surface & Motion 39) matches the live site's own counters. Names/descriptions are verbatim from the site source; categories are my mapping onto the requested taxonomy. Note: "Pulse Badge" appears twice on the site with different descriptions; the second is listed as "Pulse Badge (state)"._

- **Card Resize** `card` — Height spring expand/collapse with no JS layout thrash
- **Magnetic Button** `button` — Cursor pulls the button toward it inside a dead zone
- **Number Counter** `text` — Digit bumps and overshoots on every increment
- **Toast Overshoot** `other` — Toast slides past rest position before settling
- **Tab Pill Glide** `nav` — Tab indicator measures target width before gliding over
- **Accordion Spring** `nav` — Max-height transition with rotating chevron
- **Drag to Dismiss** `card` — Pointer-tracked drag, snaps back or flies off past threshold
- **Ripple Feedback** `button` — Radial fade-out anchored to the exact click point
- **Hold to Confirm** `button` — Press and hold; a ring fills, release early to cancel
- **Rubber-band Slider** `other` — Drag past either end and it stretches, then springs back
- **Like Burst** `button` — Heart pops and emits a radial particle ring on toggle
- **Cursor Trail** `cursor` — A chain of dots chases the pointer with eased lag
- **Push Button** `button` — Tactile depress with a real bottom edge, pure CSS
- **Star Rating** `other` — Hover previews a value, click locks it with a pop
- **Floating Label** `other` — Placeholder lifts into a label on focus, pure CSS
- **Copy Button** `button` — Icon crossfades to a check and label swaps, then reverts
- **Quantity Stepper** `other` — Value pops on each change; clamps at zero
- **Choice Chips** `other` — Toggle filter chips that pop as they switch on and off
- **PIN Input** `other` — Each digit pops and auto-advances to the next box
- **Password Meter** `other` — Segments fill and shift color as strength climbs
- **Pointer Tooltip** `cursor` — Label trails the cursor with eased follow
- **Swipe to Reveal** `other` — Drag an item horizontally to expose action buttons
- **Rotary Knob** `other` — Drag in a circle to set a value; snaps to detents on release
- **Reorderable List** `other` — Drag items up and down; others shift to make room
- **Expanding Search** `other` — Field grows on hover or focus with glide easing
- **Squish Button** `button` — Compresses on press, springs back on release
- **Toggle Pills** `button` — Selected pill pops with a spring scale
- **Value Scrubber** `other` — Drag horizontally to scrub the number
- **Speed-Dial FAB** `nav` — Actions fan out from a FAB on a staggered spring
- **Swatch Picker** `other` — Selected colour springs up with a check
- **Slide to Unlock** `other` — Drag past the latch or it springs back
- **Tag Input** `other` — Enter pops a chip in; x pops it out
- **Keycap Press** `button` — Mechanical keycap depresses on its shadow
- **Orbital Action Menu** `nav` — Actions escape a magnetic centre on hover
- **Contextual Dock** `nav` — Nearby dock controls swell in a soft focus field
- **Inertial Dial** `other` — A weighted needle catches up after the ring turns
- **Elastic Lasso** `cursor` — Drag a selection field across a dot constellation
- **Hover Intent Gate** `other` — A deliberate hover quietly unlocks the action
- **Gesture Chord** `other` — Tap keys in sequence to arm a shortcut
- **Scramble Reveal** `text` — Cycles random glyphs before settling left-to-right
- **Momentum Marquee** `scroll` — Infinite scroll strip that pauses smoothly on hover
- **Stagger Entrance** `scroll` — List items rise in sequence on scroll into view
- **Icon Morph Swap** `other` — Outgoing icon blurs and rotates out, incoming settles in
- **Underline Draw** `text` — Scale-transform underline draws left to right on hover
- **Elastic Progress** `other` — Bar overshoots target width slightly before settling
- **Delayed Tooltip** `other` — Appears after a pause, disappears instantly on exit
- **Switch Spring** `button` — Toggle knob overshoots its track position
- **Checkbox Draw** `other` — The tick is drawn with an animated SVG stroke
- **Typewriter** `text` — Types and deletes through a list with a blinking caret
- **Odometer Count-up** `text` — Eases from zero to its target when scrolled into view
- **Status Pill** `other` — Morphs idle to loading to success with icon and colour
- **Pulse Badge** `other` — Notification dot emits expanding rings forever, pure CSS
- **Success Check** `other` — Ring and tick draw themselves on, then undraw on toggle
- **Segment Loader** `other` — Stepped bars fill one after another on each run
- **Orbit Spinner** `other` — Dual-arc ring rotates on a tight infinite loop, pure CSS
- **Progress Ring** `chart` — Circular stroke eases to a new value on each run
- **Notification Slide-in** `other` — Banner drops from the top edge and settles with a spring
- **Step Progress** `nav` — Connector fills and the next node pops active
- **Undo Snackbar** `other` — Timed bar drains while you can still undo
- **Submit States** `button` — Label to bouncing dots to a settled checkmark
- **Countdown Ring** `chart` — Drains a circular ring as the seconds tick down
- **Skeleton to Content** `card` — Shimmering placeholder resolves into real content on click
- **Toast Stack** `other` — Multiple toasts stack upward and auto-dismiss in sequence
- **Indeterminate Bar** `other` — Looping sweep for unknown-length work
- **Pulse Badge (state)** `other` — Expanding ring draws the eye to new state
- **Shimmer Skeleton** `other` — Sweeping highlight over placeholder lines
- **Typing Indicator** `other` — Three dots bounce in sequence, chat-style
- **Heartbeat Monitor** `chart` — Glowing dot traces an EKG along the path
- **Battery Charge** `other` — Fill climbs as the bolt pulses
- **Signal Bars** `chart` — Bars light up in sequence while connecting
- **Badge Counter** `text` — Count springs on every add
- **Bookmark Toggle** `button` — Ribbon fills and drops in on save
- **Packet Trace** `chart` — Data pulses choose a route through a tiny network
- **Phase Lock** `chart` — Three independent signals resolve into sync
- **Checksum Bloom** `text` — A verification string resolves from noise to trust
- **Echo Receipt** `other` — Acknowledgement ripples through three trust layers
- **State Diff** `text` — The old value yields as the new state commits
- **Signal Braille** `other` — A tactile dot matrix encodes live system phases
- **Error Shake** `other` — Decaying horizontal oscillation, retriggerable
- **Confetti Burst** `other` — Radially-distributed particles with randomized arc and spin
- **Parallax Tilt** `3d` — 3D rotation tracks pointer position, glow follows beneath
- **Wave Loader** `other` — Three dots in a phase-offset vertical bounce
- **Skeleton Sweep** `other` — Gradient position animates across a fixed-size placeholder
- **Page Peel** `3d` — Top card rotates around its left edge like a page turning
- **Cursor Spotlight** `cursor` — Radial glow follows the pointer across a contained surface
- **Flip Card** `3d` — Two faces share a 3D space and rotate on click
- **Glitch Text** `text` — RGB-split copies clip and jitter on hover, pure CSS
- **Border Beam** `card` — A light sweeps the border via a masked conic gradient
- **Aurora Drift** `background` — Soft blurred colour fields drift and rotate forever
- **Shine Sweep** `card` — A diagonal light streak sweeps across on hover, pure CSS
- **Breathing Orb** `background` — A glowing orb scales and softens on an endless breath, pure CSS
- **Float Bob** `card` — A card hovers and bobs with a shadow that breathes beneath
- **Liquid Blob** `background` — An organic shape endlessly morphs its border-radius, pure CSS
- **Gradient Shimmer Text** `text` — A light band flows through clipped gradient text, pure CSS
- **Neon Glow Pulse** `text` — Text and outline breathe a soft neon halo, pure CSS
- **Equalizer Bars** `chart` — Phase-offset bars pump like an audio meter, pure CSS
- **Radar Pulse** `background` — Concentric rings expand and fade like sonar
- **Newton's Cradle** `other` — End pendulums trade momentum across the row
- **Bouncing Ball** `other` — Squash-and-stretch drop with a reactive shadow
- **Marquee Reveal** `scroll` — Cards scroll horizontally, pausing on hover, pure CSS
- **Gradient Border Morph** `card` — A rotating conic gradient creates a living border, pure CSS
- **Text Split Reveal** `text` — Each letter springs up in sequence on hover, pure CSS
- **Hover Lift** `card` — Surface rises with a soft cast shadow
- **Sheen Sweep** `card` — Specular highlight glides across on hover
- **Clip Wipe** `card` — Overlay unmasks left-to-right with clip-path
- **3D Cube Rotate** `3d` — Click to roll a face in with depth
- **Jelly Wobble** `card` — Squash-and-stretch settle on hover
- **Folding Doors** `3d` — Two panels swing open in 3D on hover
- **Before / After** `other` — Drag the handle to wipe between layers
- **Depth Stack** `card` — Layered cards fan apart on hover
- **Text Wave** `text` — A crest of motion travels across the letters
- **Caustic Glass** `background` — Shifting light pools refract beneath frosted glass
- **Chromatic Split** `text` — Type separates into prismatic channels, then snaps back
- **Warp Grid** `background` — A planar field bends inward around its focal point
- **Moiré Lens** `background` — Counter-rotating line fields create impossible depth
- **Polarized Foil** `card` — A muted spectrum appears only at grazing angles
- **Metaball Bridge** `other` — Two particles trade mass through a liquid neck

### https://ui.watermelon.sh/ — 184 components
_Full catalog enumerated via sitemap.xml (authoritative, every URL) plus the /components, /blocks, /animated-components, /dashboards index pages and 5 category pages. Coverage: 30 base component families (variant counts from the index page; a few counts were truncated in extraction and are marked 'variants'), ~132 animated components listed individually (site's own 26 categories; descriptions for pages I fetched are grounded in the site, the rest are inferred from component names and site category, not verified visually), 22 block categories totaling ~189 numbered variants (grouped, since variants are unnamed hero-1..43 style), and 2 dashboard templates. Templates section is 'Coming Soon' (empty). No auth wall encountered._

- **Accordion (16 variants)** `other` — Vertically stacked expanding/collapsing content sections
- **Alerts (30 variants)** `other` — Message/notification banners for status feedback
- **Avatar (21 variants)** `other` — Circular/rounded user image or initials display
- **Badge (24 variants)** `other` — Small status/count/label indicator
- **Breadcrumb (8 variants)** `nav` — Hierarchical page-path navigation trail
- **Button (38 variants)** `button` — Interactive action trigger in many styles
- **Button Group (12 variants)** `button` — Set of related buttons displayed together
- **Calendar (25 variants)** `other` — Date display and selection grid
- **Card (15 variants)** `card` — Structured content container
- **Checkbox (16 variants)** `other` — Toggleable option selection box
- **Collapsible (9 variants)** `other` — Show/hide content region
- **Combobox (12 variants)** `other` — Searchable select from an option list
- **Data Table (13 variants)** `other` — Sortable/managed tabular data display
- **Date Picker (13 variants)** `other` — Input with popover calendar for date selection
- **Dialog (23 variants)** `other` — Modal window for prompts and info
- **Dropdown Menu (14 variants)** `nav` — Triggered menu of actions/options
- **Form (10 variants)** `other` — Validated form layouts and fields
- **Input Mask (variants)** `other` — Text input with formatted mask (phone, card, etc.)
- **Input OTP (variants)** `other` — Segmented one-time-passcode input
- **Pagination (variants)** `nav` — Page navigation controls
- **Popover (variants)** `other` — Floating anchored content panel
- **Radio Group (variants)** `other` — Mutually exclusive single-choice options
- **Select (36 variants)** `other` — Styled option picker from a list
- **Sheet (7 variants)** `other` — Panel sliding in from a screen edge
- **Sonner (20 variants)** `other` — Opinionated stacked toast notifications
- **Switch (18 variants)** `button` — On/off toggle control
- **Table (16 variants)** `other` — Row/column data layout
- **Tabs (26 variants)** `nav` — Layered tab-panel content switcher
- **Textarea (21 variants)** `other` — Multi-line text input
- **Tooltip (variants)** `other` — Hover hint bubble
- **Aave Swap Component** `card` — DeFi token-swap panel with animated exchange flow
- **Activities Card** `card` — Card disclosing a list of recent activities
- **Adaptive Slider** `other` — Slider that adapts its form while dragging
- **Add Cash Disclosure** `other` — Expanding panel for adding funds
- **Budget Card** `card` — Animated budget/spending summary card
- **Calendar Widget** `other` — Compact animated calendar widget
- **Card Cue** `card` — Card with animated visual cue on interaction
- **Card Split Accordian** `card` — Card that splits open accordion-style
- **Card Swipe** `card` — Swipeable card stack gesture interaction
- **Carousel Navigator** `nav` — Animated navigation control for carousels
- **Carousel Slider** `other` — Sliding carousel with drag interaction
- **Changeable Pricing Section** `other` — Pricing section with animated plan switching
- **Collection Grid Disclosure** `other` — Grid that expands to reveal collection items
- **Command Search** `nav` — Cmd-K style animated command palette search
- **Compose Email Card** `card` — Animated email-compose card
- **Contextual AI Bar** `other` — Morphing AI assistant input bar
- **Continuous Pagination** `nav` — Smooth continuously-animating page indicator
- **Continuous Tabs** `nav` — Tabs with continuous sliding indicator
- **Copy Confirm** `button` — Copy button morphing into confirmation state
- **Create Community** `other` — Animated multi-step community creation flow
- **Create New Disclosure** `other` — 'Create new' button expanding into options
- **Credit Usage Card** `card` — Card visualizing credit/usage consumption
- **Deployment Card** `card` — Deploy-status card with animated states
- **Dialog Stack** `other` — Stacked layered dialogs with depth animation
- **Discrete Tabs** `nav` — Tabs with discrete stepped indicator motion
- **Dock** `nav` — macOS-style magnifying icon dock
- **Draw Signature** `other` — Freehand signature drawing input
- **Dropdown Disclosure** `nav` — Dropdown expanding with disclosure animation
- **Edit Badge** `other` — Badge that morphs into an editable state
- **Edit Profile** `other` — Inline animated profile editing panel
- **Editable Chip** `other` — Chip/tag that transforms into an input
- **Emoji Spree Choice Chips** `other` — Choice chips bursting emoji on selection
- **Event Reminders** `other` — Animated stacked event reminder list
- **Expand Details** `other` — Compact row expanding to show details
- **Expandable Event Card** `card` — Event card growing to reveal full details
- **Expandable Profile Card** `card` — Profile card expanding with more info
- **Extended Toolbar** `nav` — Toolbar extending to reveal extra tools
- **Family Receive Button** `button` — Payment-receive button with playful animation
- **Family Wallet** `other` — Shared wallet UI with animated balances
- **Feature Tour** `other` — Guided animated product feature walkthrough
- **Feedback** `other` — Animated feedback collection widget
- **Feedback Action** `other` — Quick feedback action with animated response
- **Filter Disclosure** `other` — Filter control expanding into option panel
- **Floating Disclosure** `other` — Floating panel with progressive disclosure
- **Floating Input** `other` — Input with floating label/position animation
- **Fluid Tabs** `nav` — Tabs with fluid gooey indicator motion
- **Fractional Picker** `other` — Animated picker for fractional values
- **Frequency Selector** `other` — Animated recurrence/frequency chooser
- **Fund Widget** `chart` — Investment fund widget with animated stats
- **Gooey Menu** `nav` — Menu items separating with gooey blob effect
- **Inline Action** `other` — Row-level action with inline morph animation
- **Inline Disclosure Menu** `nav` — Menu disclosing options inline in place
- **Inline Edit** `other` — Text morphing into an inline editor
- **Inline Overflow** `other` — Overflow items revealed inline on demand
- **Inline Table Control** `other` — Table row controls appearing inline
- **Inline Toast** `other` — Toast notification rendered inline in context
- **Integration Card** `card` — App-integration card with connect animation
- **Invite Disclosure** `other` — Invite flow expanding from a button
- **Journal Navigation** `nav` — Animated journal/date navigation control
- **Knob Slider** `other` — Rotary knob-style value control
- **Labeled Progress Indicator** `other` — Progress bar with animated moving label
- **Layered Progressive Disclosure** `other` — Multi-layer panels revealing progressively
- **Licence Key** `other` — License key reveal/copy interaction
- **List Stack** `other` — List items stacking/unstacking with motion
- **macOS Sidebar** `nav` — macOS-style animated collapsible sidebar
- **Meeting Card** `card` — Meeting info card with animated states
- **Minimal Carousel** `other` — Clean minimal animated image carousel
- **Morphing Button** `button` — Button morphing between action states
- **Morphing Discovery Bar** `nav` — Search/discovery bar morphing between modes
- **Morphing Sidebar Controls** `nav` — Sidebar controls morphing on toggle
- **Onboarding Checklist** `card` — Animated setup checklist card
- **Onboarding Screen** `card` — Animated onboarding screen sequence
- **Onboarding Setup** `card` — Step-by-step animated setup card
- **Pagination** `nav` — Animated page-number navigation
- **Pin Item** `other` — Pin/unpin interaction with motion
- **Pop Stepper** `button` — Stepper with popping increment animation
- **Predictive Text** `text` — Input with animated inline text prediction
- **Pricing Widget** `other` — Compact animated pricing selector widget
- **Profile Card** `card` — Animated user profile card
- **Progressive Input Stack** `other` — Stacked inputs revealed progressively
- **Quick Feedback** `other` — One-tap emoji/rating feedback micro-interaction
- **Quick Option Picker** `other` — Fast animated option picking control
- **Quick Paste** `other` — Paste action with quick animated confirm
- **Quick Switcher** `nav` — Fast animated context/workspace switcher
- **Radial Carousel** `other` — Carousel items arranged/rotating radially
- **Range Selection Slider** `other` — Dual-handle animated range slider
- **Returns Calculator Snippet** `chart` — Investment returns calculator card
- **Reveal Copy** `other` — Hidden value reveal-then-copy interaction
- **Revealing Cards** `card` — Cards revealing content with staged motion
- **Run Action Button** `button` — Run button with progress/state animation
- **Run Widget** `other` — Fitness run-tracking widget with animated stats
- **Save Toggle** `button` — Save/bookmark toggle with springy animation
- **Schedule Button** `button` — Button expanding into scheduling options
- **Schedule Date** `other` — Animated date scheduling picker
- **Scroll Island** `scroll` — Dynamic-island element reacting to scroll
- **Scrub Slider** `other` — Media-style scrubbing slider
- **Select AI Agent** `other` — Animated AI agent/model selector
- **Send Money** `other` — Money-send flow with animated confirmation
- **Share Sheet** `other` — Animated share sheet with target options
- **Shimmer Button** `button` — Button with animated shimmer sweep
- **Show QR** `other` — QR code reveal widget
- **Shuffle Pinned Item** `other` — Pinned items reordering with shuffle motion
- **Slot Picker** `other` — Time-slot picker with animated selection
- **Split Actions** `button` — Action splitting into multiple sub-actions
- **Split Button** `button` — Button splitting into secondary options
- **Split To Edit** `other` — Element splitting apart into edit mode
- **Status Picker** `other` — Animated status/state selector
- **Step Indicator** `nav` — Animated multi-step progress indicator
- **Step Pager** `nav` — Paged steps with animated transitions
- **Stepper** `button` — Increment/decrement stepper control
- **Subscription Calendar** `card` — Calendar card of upcoming subscription charges
- **Swap Currency Card** `card` — Currency swap widget with flip animation
- **Swap Form** `other` — Token/currency swap form interaction
- **Switch Disclosure** `other` — Switch expanding to disclose settings
- **Switch Mode** `button` — Animated light/dark or mode switcher
- **Tags** `other` — Animated tag add/remove interaction
- **Task Widget Disclosure** `other` — Task widget expanding to show details
- **Timed Undo Action** `button` — Undo button with countdown timer animation
- **Tooltip Navbar** `nav` — Horizontal navbar with animated tooltips
- **Trade Summary** `card` — Trade recap card with animated numbers
- **Transaction List** `other` — Animated financial transaction list
- **Tree Menu** `nav` — Hierarchical tree menu with expand animation
- **Uniswap Dialog** `other` — Uniswap-style animated swap dialog
- **Vertical Tooltip Navbar** `nav` — Vertical nav rail with animated tooltips
- **View On Map** `other` — Card expanding into an animated map view
- **Voice Chat Disclosure** `other` — Voice chat control expanding into panel
- **Voice Note** `other` — Voice note recording with waveform animation
- **Voice Transcribe** `text` — Live voice-to-text transcription animation
- **Waveform Scrub** `other` — Audio waveform with scrubbing interaction
- **Weight Widget** `chart` — Weight tracking widget with animated scale
- **Announcement blocks (9)** `other` — Top-of-page announcement bar sections
- **Auth blocks (13)** `other` — Sign-in/sign-up page sections
- **Bento blocks (2)** `other` — Bento-grid feature layout sections
- **Blog blocks (4)** `other` — Blog listing/article sections
- **Career blocks (4)** `other` — Job listing/careers page sections
- **Contact blocks (7)** `other` — Contact form/info sections
- **CTA blocks (5)** `other` — Call-to-action banner sections
- **Error blocks (7)** `other` — 404/error page sections
- **FAQ blocks (6)** `other` — Accordion FAQ sections
- **Feature blocks (5)** `other` — Product feature showcase sections
- **File-upload blocks (5)** `other` — Drag-and-drop file upload sections
- **Footer blocks (31)** `nav` — Site footer layouts
- **Hero blocks (43)** `other` — Landing-page hero sections
- **Integrations blocks (5)** `other` — Integration/logo grid sections
- **Navigation blocks (7)** `nav` — Header/navbar layouts
- **Newsletter blocks (5)** `other` — Email newsletter signup sections
- **Notification blocks (4)** `other` — Notification panel/list sections
- **Pricing blocks (5)** `other` — Pricing table sections
- **Stats blocks (4)** `chart` — Metric/statistics display sections
- **Team blocks (5)** `other` — Team member grid sections
- **Testimonials blocks (4)** `other` — Customer quote/testimonial sections
- **Widget blocks (9)** `other` — Standalone mini-widget sections
- **Portfolio Dashboard** `chart` — Full portfolio analytics dashboard template with charts and tables
- **Web3 Dashboard** `chart` — Crypto/Web3 dashboard template with charts and tables

### https://21st.dev/home — 231 components
_Partial at item level, full at catalog-structure level. 21st.dev is a community marketplace, not a fixed library: its sitemap contains 8,520 individual component URLs (saved to /private/tmp/claude-503/-Users-nikolassapalidis/b8828157-8862-4599-8ad1-ae5fc760231d/scratchpad/sitemap-urls.txt) across ~110 tag pages and hundreds of authors, so per-item enumeration of all 8.5k with descriptions is impossible within ~20 fetches. What IS fully enumerated: the complete official catalog taxonomy (79 collections with exact item counts, listed first above, verified from the site nav) plus the extra sections (Templates, ASCII Art, Shaders, Gradients, Themes). Item-level entries above (~140 rows covering ~250 named components) were verified by rendering the text, background, cursor, 3d, data-visualization, scroll-area, globe and navbar-navigation category pages with a real browser; descriptions for well-known components (aceternity/magicui/motion-primitives/fancy/reactbits) come from known previews, others are name-derived. The remaining thousands (buttons 588, cards 503, inputs 362, heroes 306, etc.) are counted but not individually listed._

- **Backgrounds (collection, 103 items)** `background` — Animated/decorative full-bleed background components
- **Borders (collection, 32)** `other` — Animated and decorative border treatments
- **Announcements (13)** `other` — Announcement bars and pills
- **ASCII Art (collection)** `other` — ASCII-art generators and renders
- **Calls to Action (69)** `other` — CTA marketing sections
- **Clients (27)** `other` — Client/logo cloud sections
- **Comparisons (8)** `other` — Before/after and plan comparison blocks
- **Docks (27)** `nav` — macOS-style magnifying docks
- **FAQs (39)** `other` — FAQ accordion sections
- **Features (112)** `other` — Feature grid/section blocks
- **Footers (53)** `nav` — Site footer blocks
- **Galleries (40)** `other` — Image gallery layouts
- **Gradients (collection)** `background` — Curated gradient swatches/presets
- **Heroes (306)** `other` — Landing-page hero sections
- **Hooks (26)** `other` — React utility hooks
- **Images (123)** `other` — Image display/effect components
- **Maps (50)** `chart` — Map visualizations
- **Marquees (16)** `scroll` — Infinite scrolling marquee strips
- **Navigation Menus (17)** `nav` — Navbar/menu marketing blocks
- **Pricing Sections (53)** `other` — Pricing table sections
- **Scroll Areas (38)** `scroll` — Custom scroll containers and scroll effects
- **Shaders (collection)** `background` — WebGL/GLSL shader backgrounds and effects
- **Stats & KPIs (28)** `chart` — Stat/KPI number blocks
- **Steppers (24)** `other` — Step/progress wizards
- **Team Sections (15)** `other` — Team member grids
- **Testimonials (55)** `other` — Testimonial/review sections
- **Texts (140)** `text` — Animated typography effects
- **Timelines (20)** `scroll` — Vertical/horizontal timelines
- **Videos (31)** `other` — Video players/embeds
- **Accordions (152)** `other` — Expandable accordion lists
- **AI Chats (77)** `other` — AI chat UIs: messages, reasoning, tool calls
- **Alerts (80)** `other` — Alert/callout components
- **Avatars (147)** `other` — Avatar and avatar-stack components
- **Badges (132)** `other` — Badge/label components
- **Buttons (588)** `button` — Button variants incl. spotlight, glow, animated hover
- **Calendars (174)** `other` — Calendar widgets
- **Cards (503)** `card` — Content/product/profile cards incl. animated variants
- **Carousels (78)** `other` — Sliding/3D carousels
- **Charts & Data Viz (94)** `chart` — Charts, graphs, dashboards
- **Checkboxes (116)** `other` — Checkbox variants
- **Cursors (52)** `cursor` — Custom cursor followers, trails, magnetic/spotlight effects
- **Dashboards (36)** `chart` — Full dashboard layouts
- **Date Pickers (56)** `other` — Date/range pickers
- **Dialogs / Modals (94)** `other` — Modal and dialog components
- **Dropdowns (85)** `nav` — Dropdown menus
- **Empty States (16)** `other` — Empty-state placeholders
- **File Trees (30)** `other` — File tree explorers
- **File Uploads (9)** `other` — Upload/download widgets
- **Forms (185)** `other` — Form layouts and field groups
- **Globes (20)** `3d` — 3D rotating globes
- **Grids & Bento (62)** `other` — Bento-grid layouts
- **Icons (23)** `other` — Icon sets/animated icons
- **Inputs (362)** `other` — Text input variants
- **Links (47)** `text` — Animated link/underline effects
- **Lists (39)** `other` — List components
- **Menus (107)** `nav` — Context and app menus
- **Notifications (49)** `other` — Notification lists/toasts
- **Numbers (27)** `text` — Animated number counters/flip clocks
- **Onboarding (15)** `other` — Onboarding flows
- **Paginations (53)** `nav` — Pagination controls
- **Popovers (55)** `other` — Popover components
- **Profiles (45)** `card` — Profile cards/headers
- **Progress (46)** `other` — Progress bars/rings
- **Radio Groups (70)** `other` — Radio group variants
- **Search Bars (56)** `other` — Search inputs with suggestions
- **Selects (146)** `other` — Select/combobox variants
- **Sidebars (24)** `nav` — App sidebars
- **Sign Ins (34)** `other` — Login forms
- **Sign ups (24)** `other` — Registration forms
- **Sliders (105)** `other` — Range sliders and content sliders
- **Spinner Loaders (37)** `other` — Loading spinners
- **Tables (76)** `chart` — Data tables
- **Tabs (146)** `nav` — Tab switchers incl. animated indicators
- **Tags (11)** `other` — Chip/tag components
- **Text Areas (79)** `other` — Textarea variants
- **Toasts (33)** `other` — Toast notifications
- **Toggles (83)** `other` — Switch/toggle variants
- **Tooltips (81)** `other` — Tooltip variants
- **Templates (collection)** `other` — Full page/site templates
- **Themes (collection)** `other` — Color theme presets
- **text-scramble (ibelick)** `text` — Characters scramble then resolve into the final text
- **text-shimmer (ibelick)** `text` — Shimmering highlight sweeps across text
- **text-loop (ibelick)** `text` — Cycles through words with animated transitions
- **text-effect (ibelick)** `text` — Per-character/word entrance animation presets
- **typewriter-effect (manuarora700)** `text` — Types text out character by character with cursor
- **flip-words (manuarora700)** `text` — Rotating word flips inside a sentence
- **text-generate-effect (manuarora700)** `text` — Words fade/blur in sequentially
- **text-reveal-card (manuarora700)** `text` — Mouse-swipe reveals hidden text layer on a card
- **container-text-flip (manuarora700)** `text` — Word flips inside an animated resizing container
- **pointer-highlight (manuarora700)** `text` — Pointer-drawn highlight box around key text
- **text-reveal (dillionverma)** `text` — Scroll-driven word-by-word reveal
- **sparkles-text (dillionverma)** `text` — Text with animated sparkle particles
- **morphing-text (dillionverma)** `text` — Gooey morph between words
- **hyper-text (dillionverma)** `text` — Letters scramble on hover before settling
- **blur-fade / blur-in / fade-text / word-fade-in (dillionverma)** `text` — Blur-and-fade entrance text animations
- **word-rotate / word-pull-up / flip-text / gradual-spacing (dillionverma)** `text` — Word rotation, pull-up, flip and letter-spacing reveals
- **typing-animation (dillionverma)** `text` — Auto-typing text animation
- **comic-text (dillionverma)** `text` — Comic-book style styled text
- **vertical-cut-reveal (danielpetho)** `text` — Letters revealed via vertical clipping cut
- **letter-swap / random-letter-swap (danielpetho)** `text` — Hover swaps letters with slide animation
- **variable-font-hover-by-random-letter (danielpetho)** `text` — Variable-font weight animates per letter on hover
- **text-along-path (danielpetho)** `text` — Text flows along an SVG path (circle/auto/scroll variants)
- **text-highlighter (danielpetho)** `text` — Scroll-triggered marker-highlight over text
- **gooey-text-morphing (victorwelander)** `text` — Liquid gooey morph between words
- **gradient-heading (cult-ui)** `text` — Gradient-filled display heading
- **text-rewind (kokonutd)** `text` — Text rewinds/replays like tape
- **hand-writing-text (kokonutd)** `text` — SVG handwriting draw-on effect
- **wave-text (kokonutd)** `text` — Letters oscillate in a wave
- **matrix-text (kokonutd)** `text` — Matrix-style falling glyph text
- **split-text / glitch-text / decrypted-text / rotating-text / circular-text / fuzzy-text (reactbits)** `text` — Split, glitch, decrypt, rotate, circular-layout and fuzz text effects
- **scroll-float / scroll-reveal (reactbits)** `scroll` — Scroll-linked floating/reveal text
- **vapour-text-effect (jatin-yadav05)** `text` — Text dissolves into vapor particles
- **text-particle / interactive-text-particle (designali-in)** `text` — Text rendered as interactive particles
- **liquid-text (designali-in)** `text` — Liquid distortion over text
- **gradient-text (designali-in)** `text` — Animated gradient text fill
- **neon-rgbtext-effect (xubohuah)** `text` — Neon RGB glow text
- **vaporize-animation-text (xubohuah)** `text` — Text vaporizes away on trigger
- **particle-text-effect (xubohuah)** `text` — Canvas particle-formed text
- **typewriter-text (preetsuthar17)** `text` — Typewriter typing animation
- **blurred-stagger-text (preetsuthar17)** `text` — Staggered blur-in letters
- **infinite-text-marquee (preetsuthar17)** `text` — Endless scrolling text strip
- **animated-shiny-text (anurag-mishra22)** `text` — Shine sweep across text
- **circular-reveal-heading (anurag-mishra22)** `text` — Heading revealed with circular mask
- **scramble-hover (larsen66)** `text` — Hover scrambles then restores letters
- **text-inertia (lyanchouss)** `text` — Text follows pointer with inertia physics
- **gooey-filter background (danielpetho)** `background` — SVG gooey filter applied to background blobs
- **aurora-background (manuarora700)** `background` — Soft moving aurora gradient wash
- **background-beams (manuarora700)** `background` — Animated light beams tracing paths
- **background-beams-with-collision (manuarora700)** `background` — Beams that collide and explode
- **wavy-background (manuarora700)** `background` — Canvas waves undulating behind content
- **vortex (manuarora700)** `background` — Swirling particle vortex
- **sparkles (manuarora700)** `background` — Configurable sparkle particle field
- **shooting-stars + stars-background (manuarora700)** `background` — Starfield with shooting star streaks
- **meteors (manuarora700)** `background` — Diagonal meteor streaks falling
- **background-boxes (manuarora700)** `background` — Grid of boxes highlighting on hover
- **background-gradient-animation (manuarora700)** `background` — Animated multi-color gradient blobs
- **canvas-reveal-effect (manuarora700)** `background` — Dot-matrix canvas reveal on hover
- **hero-highlight (manuarora700)** `background` — Dotted background with text highlight
- **retro-grid (dillionverma)** `background` — Scrolling perspective retro grid
- **animated-grid-pattern (dillionverma)** `background` — Grid with animated highlighted squares
- **flickering-grid (dillionverma)** `background` — Grid of flickering squares
- **particles (dillionverma)** `background` — Interactive particle field
- **warp-background (dillionverma)** `background` — Warp-speed perspective beams frame
- **grid-pattern / dot pattern (dillionverma)** `background` — Static SVG grid/dot backgrounds
- **shape-landing-hero (kokonutd)** `background` — Floating geometric shapes hero background
- **background-circles / background-paths / beams-background (kokonutd)** `background` — Animated circles, flowing paths and beams backgrounds
- **pixel-trail (danielpetho)** `background` — Pixels light up along mouse trail
- **animated-gradient-with-svg (danielpetho)** `background` — SVG-based animated gradient blobs
- **glow (mikolajdobrucki)** `background` — Soft glow accent behind content
- **tiles (lukacho)** `background` — Hover-reactive tile grid
- **entropy / wave-background / spiral-animation (xubohuah)** `background` — Generative entropy, wave and spiral canvas backgrounds
- **etheral-shadow (jatin-yadav05)** `background` — Ethereal animated shadow/smoke background
- **parallax-cosmic-background (easemize)** `background` — Parallax cosmic starfield
- **magnetic-cursor / magnetic buttons** `cursor` — Elements pull the cursor magnetically
- **spotlight-cursor** `cursor` — Spotlight follows the mouse over content
- **blob-cursor (reactbits)** `cursor` — Gooey blob follows the pointer
- **smokey-cursor-effect** `cursor` — Fluid smoke simulation trails the cursor
- **image-cursor-trail** `cursor` — Images spawn along the mouse path
- **spider-cursor** `cursor` — Web/line network attaches to pointer
- **glitch-cursor** `cursor` — Glitch distortion around pointer
- **liquid-cursor** `cursor` — Liquid distortion follows pointer
- **inverted-cursor** `cursor` — Blend-mode cursor inverts content beneath
- **cursor-follower / cursor-follow / custom-cursor** `cursor` — Smooth-follow replacement pointers
- **cursor with-spring (ibelick)** `cursor` — Springy custom cursor follow
- **ribbons (reactbits)** `cursor` — Ribbon trails stream behind cursor
- **mouse-following-line** `cursor` — Line/path drawn toward pointer
- **tube-cursor / morphing-cursor / crystal-cursor / arrow-cursor / magic-cursor** `cursor` — Stylized cursor shapes with morph/glow treatments
- **halftone-trail / cursor-dither-trail** `cursor` — Halftone/dither pixels trail the mouse
- **neural-noise-cursor / fluid-canvas / tidal-cursor** `cursor` — Shader noise/fluid fields reacting to cursor
- **mouse-spark / draw-with-cursor** `cursor` — Sparks or drawn strokes follow the pointer
- **svg-mask-effect (aceternity)** `cursor` — Mask follows mouse revealing hidden layer
- **variable-font-and-cursor (danielpetho)** `cursor` — Font weight shifts near the cursor
- **text-repel (componentry)** `cursor` — Letters repel away from the pointer
- **3d-card / 3d-card-effect (aceternity-style)** `3d` — Perspective tilt card reacting to mouse
- **3d-tilt-card / 3d-perspective-card / dimensional-card / lunar-gravity-card** `3d` — Tilt/parallax 3D card variants
- **3d-flip-card / animated-3d-card / 3d-image-card / 3d-book-testimonial** `3d` — Flip, image and book-style 3D cards
- **3d-marquee (aceternity)** `3d` — 3D-angled scrolling image marquee wall
- **3d-carousel / cosmos-3d-orbit-gallery / 3d-orbit-gallery** `3d` — Items orbit in 3D carousel rings
- **interactive-globe** `3d` — Drag-rotatable 3D globe
- **threejs-particles-waves** `3d` — Three.js particle wave field
- **fire-sphere / 3d-orb / geometric-orb / geometric-sphere / gradient-orb** `3d` — Animated 3D spheres and orbs
- **old-television-shader / celestial-sphere-shader** `3d` — WebGL shader scenes (CRT TV, celestial sphere)
- **volumetric-beams / volumetric-studio / woven-light-hero** `3d` — Volumetric light 3D hero scenes
- **interactive-3d-character / robot-hero** `3d` — Interactive 3D character hero scenes
- **3d-parallax-unfurling-gallery / 3d-gallery-photography** `3d` — Parallax 3D photo galleries
- **scroll-morph-hero / halide-topo-hero** `3d` — Scroll-driven 3D morphing hero sections
- **ascii-renderer** `3d` — Renders 3D scene as ASCII characters
- **3d-button / tilt / 3d-wrapper** `3d` — 3D press/tilt primitives
- **siri-orb** `3d` — Siri-style animated glowing orb
- **celestial-orrery** `3d` — Orbiting planetary system animation
- **cobe-globe family (~13 variants)** `3d` — Cobe WebGL dotted globes: pulse, interactive, live, analytics, weather, bars, satellites, labels, flights, stickers, polaroids, cdn
- **wireframe-dotted-globe / globe-hero / globe-feature-section** `3d` — Dotted/wireframe globes for hero and feature sections
- **orbiting-circles (magicui)** `3d` — Icons orbit on circular paths
- **data-grid-table (+18 variants)** `chart` — Full data grid: sortable, resizable, pinnable, draggable, striped, dense variants
- **statistics-card series (1-15)** `chart` — KPI stat cards with trends
- **area-chart-medium / bar-chart / horizontal-bar / multi-series-bar / bar-stack** `chart` — Area and bar chart blocks in several sizes
- **radar-chart / radial-bar / funnel-chart-big / layered-chart-xl / threshold** `chart` — Radar, radial, funnel, layered and threshold charts
- **heat-map (xl/middle) / heatmaps** `chart` — Heatmap grids
- **word-cloud / dendrogram** `chart` — Word cloud and hierarchical dendrogram viz
- **mapcn-map-arc / mapcn-map-cluster-layer / flightcn-flight-airport** `chart` — Map visualizations with arcs, clusters, flight routes
- **visualize-booking / dashboard-1 / crypto-dashboard / incident-report blocks** `chart` — Composite analytics dashboard blocks
- **excel-style-table / info-cards-table** `chart` — Spreadsheet-style and card-augmented tables
- **ai-swarm-visualization** `chart` — Animated AI agent swarm graph
- **sticky-scroll-reveal (aceternity)** `scroll` — Content panes swap as you scroll a sticky section
- **scroll-progress (magicui)** `scroll` — Page scroll progress bar
- **container-scroll-animation (aceternity)** `scroll` — Screenshot tilts/scales up while scrolling
- **parallax-scroll / zoom-parallax** `scroll` — Parallax image columns and zooming parallax
- **scroll-based-velocity / scroll-velocity-text** `scroll` — Marquee speed tied to scroll velocity
- **text-gradient-scroll / reading-text-reveal / text-scroll-read** `scroll` — Text tints/reveals progressively as you read-scroll
- **scroll-expansion-hero** `scroll` — Hero media expands on scroll
- **scroll-image-tunnel** `scroll` — Scroll flies through an image tunnel
- **scroll-linked-spring-animation** `scroll` — Spring physics bound to scroll position
- **sticky-section-tabs / scroll-navigation-menu** `scroll` — Sticky tabs/nav synced to scroll sections
- **marquee / marquee-effect / vertical-marquee / blurred-marquee** `scroll` — Infinite marquee strips, vertical and blurred variants
- **timeline / modern-timeline (aceternity)** `scroll` — Scroll-animated timelines
- **scroll-and-swap-text** `scroll` — Text swaps letters on scroll
- **8bit-scroll-area** `scroll` — Retro 8-bit styled scrollbar container
- **tubelight-navbar** `nav` — Nav with glowing tubelight active indicator
- **anime-navbar** `nav` — Playful animated navbar with mascot motion
- **floating-navbar / floating-nav / floating-header** `nav` — Detached pill navbars that hide/show on scroll
- **expandable-tabs** `nav` — Icon tabs expand to show labels
- **navbar-menu (aceternity)** `nav` — Hover mega-menu navbar
- **hover-gradient-nav-bar / futuristic-nav / future-navbar** `nav` — Gradient/sci-fi styled navbars
- **radial-menu / circle-menu** `nav` — Circular fan-out menus
- **shifting-dropdown** `nav` — Dropdown panel morphs between tabs
- **toolbar-dock** `nav` — Dock-style magnifying toolbar
- **modern-mobile-menu / bottom-menu** `nav` — Mobile bottom navigation menus
- **8bit-navigation-menu** `nav` — Retro pixel-art navigation
- **nested-dashboard-menu / app-menu-bar / menubar** `nav` — App-style nested menus and menu bars
- **animated-menu / navigation-menu variants (1-5)** `nav` — Assorted animated navigation menus
- **header variants (simple, with-search, promote, header 1-3)** `nav` — Site header blocks

### https://vibeui.online/ — 92 components
_Full catalog enumerated. VibeUI is not a component/effects library but a library of 92 UI section prompts (for AI generation), all rendered on the single landing page; sitemap confirms only / and /about plus hash anchors per category. All 92 entries extracted from raw HTML (the 'All' tab duplicates the 15 category sections, deduplicated here). Categories map to the site's own sections (auth, pricing, features, hero, cta, stats, nav, testimonials, footer, faq, dashboard, onboarding, blog, contact, bonus/other); requested categories (text/background/scroll/chart/button) applied where they genuinely fit. No cursor or 3d components exist on the site._

- **Split-screen with visual** `auth-form` — Auth form fields on one side, product screenshot or branded graphic panel on the other
- **Centered card on background** `auth-form` — Floating auth card with heading, email, password, CTA, and sign-up link on a full-page background
- **Minimal single-column** `auth-form` — No card container — logo, heading, fields, and CTA stacked cleanly
- **Social-first stack** `auth-form` — Social login buttons primary at top, email/password secondary below a divider
- **Magic link only** `auth-form` — Single email input and one CTA — no password field, magic link flow
- **Multi-step wizard** `auth-form` — Progress indicator at top, signup split across 2-3 steps with back/next
- **Classic 3-tier cards** `pricing` — Three side-by-side pricing tier cards with the middle highlighted as recommended
- **Monthly/annual toggle** `pricing` — Billing toggle at top with a 'save X%' badge, prices update on selection
- **Comparison table** `pricing` — Plans as columns, features as rows with checkmarks and dashes
- **Single-tier hero** `pricing` — One large centered card with price, full feature list, and prominent CTA
- **Usage slider** `pricing` — Interactive usage slider that dynamically updates the displayed price
- **Credit pack grid** `pricing` — Grid of credit pack tiles with amounts, prices, and badges
- **Free vs Pro split** `pricing` — Two-column split with strong visual contrast between Free and Pro
- **Stacked mobile-first cards** `pricing` — Full-width stacked pricing cards, one per tier, each with its full feature list
- **3-column icon grid** `features` — Three-column grid with icon, feature title, and short description
- **Bento grid** `features` — Asymmetric tiles of varying sizes, important features in larger tiles
- **Alternating rows** `features` — Zigzag rows with text on one side and visual on the other, alternating
- **Tabbed features** `features` — Tabbed interface where each tab shows a different feature with visual
- **Accordion list** `features` — Vertical accordion of collapsible feature rows with full description on expand
- **Scroll-triggered reveal** `scroll` — Sticky visual on one side, feature descriptions scroll on the other
- **Icon wall** `features` — Dense grid of small icon tiles — compact cards with icon and short label
- **Before/after comparison** `features` — Side-by-side comparison showing without vs. with the product
- **Centered text + screenshot** `hero` — Centered heading, subheading, CTAs at top, large product screenshot below
- **Split hero** `hero` — Heading and CTAs on one side, product mockup on the other
- **Video background** `background` — Looping video or animated background behind heading and CTAs
- **Animated gradient mesh** `background` — Animated gradient mesh background with heading and CTAs on top
- **Terminal/code hero** `hero` — Terminal or code-editor visual as central element with animated typing
- **Floating UI elements** `hero` — Multiple product screenshots or UI cards floating around the central heading
- **Big bold typography** `text` — Oversized, bold typography as the focal point with minimal supporting elements
- **Hero with inline form** `hero` — Email capture form embedded directly under the heading
- **Full-bleed gradient banner** `cta` — Full-bleed section with gradient background, centered heading, and CTA
- **Split CTA card** `cta` — Contained card with heading on one side and CTA button on the other
- **Sticky bottom bar** `cta` — Fixed bar at viewport bottom with message, CTA, and dismiss option
- **Patterned background** `background` — Subtle pattern or texture behind centered message and button
- **Dual-CTA** `button` — Two buttons side by side — primary and secondary action
- **Inline email capture CTA** `cta` — Email input and submit button inline beneath heading and subheading
- **Testimonial + CTA combo** `cta` — Customer quote above the heading and CTA for social proof
- **Horizontal 4-column numbers** `stats` — Four large numbers in a row with descriptive labels underneath
- **Animated count-up** `stats` — Numbers animate counting up when scrolled into view
- **Icon + stat pairs** `stats` — Relevant icon paired with each number and label
- **Logo bar + stats combo** `stats` — Customer logos above or beside key stats like '10k+ users'
- **Vertical stacked stats** `stats` — Vertical stat stack suited for a narrow column or sidebar
- **Stats over full-bleed image** `stats` — Full-bleed image or gradient background with stats overlaid
- **Card-based stats** `stats` — Row of individual bordered cards, each containing one stat
- **Minimal logo + links + CTA** `nav` — Navbar: logo left, links center/right, CTA button far right
- **Centered logo split nav** `nav` — Logo centered with navigation links split evenly on either side
- **Mega menu** `nav` — Full-width mega menu dropdown with categorized sub-links
- **Sidebar nav** `nav` — Vertical sidebar with logo at top, links stacked, user section at bottom
- **Floating pill nav** `nav` — Rounded pill nav detached from top edge, centered with subtle shadow
- **Transparent over hero** `nav` — Nav transparent when over hero, becomes solid on scroll
- **Command-palette nav** `nav` — Prominent search/command-palette input (Cmd+K) as central nav element
- **Bottom mobile nav** `nav` — Bottom tab bar for mobile — icons with labels, app-style
- **3-column quote cards** `testimonials` — Three side-by-side quote cards with photo, name, role, and quote
- **Marquee scroll** `testimonials` — Infinite horizontally scrolling marquee of testimonial tiles
- **Masonry grid** `testimonials` — Masonry/Pinterest-style grid of varied-height testimonial cards
- **Single featured testimonial** `testimonials` — One large featured quote with photo, name, role, and company logo
- **Video testimonial grid** `testimonials` — Grid of clickable video thumbnails with play icon, name, and company
- **Tweet wall** `testimonials` — Cards styled like X/Twitter posts — avatar, handle, timestamp, content
- **Logo cloud** `testimonials` — Clean grid or row of customer/partner logos with heading above
- **Carousel with dots** `testimonials` — Single-testimonial carousel with navigation dots or arrows
- **Multi-column sitemap** `footer` — Footer with grouped links (Product, Company, Resources), logo, tagline, copyright
- **Minimal single-row** `footer` — Footer: logo, a few key links, and copyright text inline in one row
- **Giant logo footer** `footer` — Huge wordmark as footer centerpiece with links arranged around it
- **Newsletter-first footer** `footer` — Newsletter signup as dominant element with nav links secondary below
- **Status indicator footer** `footer` — Live system status indicator alongside the usual links and logo
- **Accordion single column** `faq` — Single-column FAQ accordion — each question expands its answer on click
- **Two-column accordion** `faq` — FAQ questions split across two columns in accordion layout
- **Chat-bubble Q&A** `faq` — Questions and answers in alternating chat bubbles
- **Searchable FAQ** `faq` — Search bar at top that filters the question list as user types
- **Categorized tabs** `faq` — Category tabs at top that switch between filtered sets of FAQ questions
- **Sidebar + main content** `dashboard` — Dashboard: vertical sidebar with nav, main content area filling the rest
- **Top nav + content** `dashboard` — Dashboard: horizontal top nav with full-width content beneath, no sidebar
- **Three-pane layout** `dashboard` — Sidebar, middle list/feed pane, and detail panel on the right
- **Kanban board** `dashboard` — Columns representing stages with draggable cards within each
- **Metric cards + chart** `chart` — Grid of metric cards at top with a larger chart below
- **Empty state** `dashboard` — Centered illustration or icon, heading, and CTA to get started
- **Checklist onboarding** `onboarding` — Checklist of setup tasks with progress indicators and expandable items
- **Tour tooltips** `onboarding` — Guided tour with tooltip callouts pointing to UI elements
- **Video walkthrough modal** `onboarding` — Modal with embedded walkthrough video and get-started CTA
- **Illustrated empty state** `onboarding` — Custom illustration, short heading, supporting sentence, and CTA
- **Magazine grid** `blog` — Large featured post at top with smaller cards in a grid beneath
- **Single column reading view** `blog` — Clean single-column layout with generous line spacing and readable width
- **Sidebar TOC** `blog` — Sticky table of contents sidebar highlighting current section on scroll
- **Card grid with filters** `blog` — Post card grid with category filter tags above
- **Split form + details** `contact` — Contact form on one side, details (email, phone, address) on the other
- **Support channel cards** `contact` — Grid of cards for support channels — chat, email, docs, community
- **Full-page minimal form** `contact` — Full-page centered form with heading, minimal fields, and one submit button
- **Comparison table (us vs competitors)** `other` — Your product vs competitors with checkmarks and dashes
- **Feature announcement banner** `other` — Slim bar at top with 'new' tag, short message, link, and dismiss
- **Cookie banner** `other` — Bottom-corner card with message, accept/reject, and settings link
- **404 page** `other` — 'Page not found' message, explanation, and CTAs for home/support
- **Loading state** `other` — Skeleton layout mirroring final content structure with shimmer animation

### https://www.framer.com/marketplace/components/ — 217 components
_Partial by necessity: the sitemap (framer.com/sitemap.xml) enumerates 7,704 component detail pages, far beyond the fetch budget. Covered: all 12 category index pages (carousels, typography, buttons, data, embeds, sections, backgrounds, layout, interactions, forms, navigation, cms — these SSR only a few featured items each), the featured page, and 220 component detail pages (every component surfaced on the index pages plus the first ~165 sitemap entries, alphabetical from 3d-*). 217 unique components reported with real names and marketplace descriptions (3 fetched slugs 404'd to a generic page and were dropped). Names/descriptions come from each page's <title> and meta description; categories mapped from site categories plus name keywords into the requested vocabulary. Tavily extract hit a plan usage limit mid-run, so enumeration used sitemap + direct HTTP fetches. The remaining ~7,480 components are enumerable by slug from the sitemap but were not fetched for descriptions._

- **3D Bubble Text** `3d` — Soft, inflated 3D letters texts
- **3D Can** `3d` — Interactive 3D can showcase
- **3D Cube CMS Carousel** `3d` — Unique cube-rotation carousel
- **3D Curved Carousel** `3d` — Rotating 3D infinite carousel
- **3D Gallery Tunnel** `3d` — Four path modes with autoplay
- **3D Infinite Curl** `3d` — Seamless scrolling image band
- **3D IPhone Mockup** `3d` — 3D iPhone mockup for media showcase
- **3D Scroll Tunnel** `3d` — Powerful 3D scroll tunnel
- **Accordion Gallery X** `other` — Click-to-expand cinematic gallery
- **Account Dropdown Pro** `nav` — Nested account dropdown
- **AI Prompt Pro** `other` — AI prompt box and chat input
- **Animated GlassFolder** `other` — Interactive frosted glass folder
- **Animated Gradient** `background` — Animated gradient background
- **Announcement Bell** `other` — A what's-new notification bell
- **Arrow Motion Button** `button` — Animated arrow hover interaction
- **Aura AI Chat** `other` — Customizable modern AI chat widget
- **AuroraShimmer** `text` — Eye-catching gradient shimmer text
- **Back to Top (Ring)** `scroll` — Back-to-top button with scroll progress ring
- **Back Trigger** `other` — Browser back-navigation trigger
- **Before After Sliderr** `other` — Free before/after comparison slider
- **Best-Loder** `other` — Page loader animation
- **Better Carousel** `other` — Advanced carousel and slider
- **Calendar Showcase** `other` — Interactive date picker
- **Cards Carousel X** `card` — Cinematic filmstrip card carousel
- **Cards Hover Section** `card` — Interactive hover gallery and modals
- **Careers Section** `other` — Filterable job roles list, no CMS
- **Cinema Relax Hero** `other` — Multi-scene video hero
- **Cinematic Hero** `other` — Hero section with mouse-scrubbing
- **Cinematic Slider** `other` — High-end cinematic image slider
- **CineStrip Slider** `scroll` — Cinematic parallax slider
- **Circular Reel** `other` — Turns any list of items into a circular reel
- **Circular UI Slider** `other` — Circular slider connecting any UI
- **Classic Clock** `other` — A clock that fits your design
- **CMS Arc Gallery** `other` — CMS-powered arc carousel
- **CMS Arc Video Slider** `other` — CMS-powered video carousel
- **CMS Item Count** `other` — Displays total number of CMS items
- **CMS Masonry Lightbox** `other` — CMS masonry grid with lightbox
- **CMS Pivot Carousel** `other` — CMS-powered image carousel
- **CMS Scroll Blinds** `scroll` — CMS scroll blind transitions
- **CMS Star Rating** `other` — Displays ratings and review counts
- **CMS Warp Gallery** `scroll` — Infinite scroll warp gallery (CMS)
- **Compact Navigation** `nav` — Customizable compact control layout
- **Compare Ring** `chart` — Comparison arcs that fill, dots that race
- **Computer SaaS Hero** `other` — Header section for tech startups
- **Connect Hub** `other` — Brand-at-center integrations diagram
- **Coolwriter** `text` — Playful typewriter effect
- **Copy Flow** `button` — Animated copy-to-clipboard button
- **Country Phone Input** `other` — Native-like country phone input field
- **Crypto** `other` — Crypto data component
- **CursorCoordinates** `cursor` — Precision crosshair cursor readout
- **Curtain Reveal Anim** `other` — Curtain reveal preloader animation
- **Custom FAQ Accordion** `other` — FAQ accordion without manual work
- **Data Strings** `background` — Generative flowing line background
- **Diagonal Slideshow** `other` — Image slider on the diagonal
- **Distorto Slider FX** `3d` — WebGL distortion slider
- **Docs Kit** `other` — Whole docs page, no CMS
- **Dot Pulse** `background` — Animated field of pulsing dots
- **Dragable Ticker** `scroll` — Draggable ticker strip
- **Dual Axis Compare** `other` — 2-way image comparison slider
- **DualWaveText** `text` — Dual-layer wave text motion effect
- **Dynamic Info** `other` — Dynamic info display
- **Editorial Card Grid** `card` — Art gallery grid, hover to reveal
- **Editorial Focus Card** `card` — One card, two moods, flip between
- **Editorial Galley** `3d` — Draggable 3D image orbit
- **Editorial Ticker** `scroll` — Content ticker with multiple display modes
- **Effect 3D Images** `3d` — Interactive 3D image hover effects
- **Ehteral-shadow** `background` — Ethereal motion background
- **Fan Out** `scroll` — Photo deck dealt by scroll
- **FAQ Flow** `other` — Modern FAQ section
- **Feature List - Menu** `nav` — Crafted feature-list menus
- **Filtered Gallery** `other` — Filterable lightbox gallery
- **Flip Scroll Ticker** `scroll` — Flipping scroll ticker
- **Floating Parallax** `scroll` — All-in-one parallax engine
- **Flowing Menu1** `nav` — Smooth hover navigation
- **FlowMenu** `nav` — Navbar with animated menu
- **Flux Grid** `3d` — 3D interactive grid pattern
- **FullScreenMenu** `nav` — Full-screen overlay menu
- **G-Bars** `background` — Gradient bars background
- **Glass Shatter Reveal** `other` — Glass shatter reveal effect
- **Glass Showcase Pro** `scroll` — Scroll glass box with particle reveal
- **Glass** `other` — Glassmorphism surface effect
- **Glassy button** `button` — Glassmorphic button
- **Glimpse Card** `card` — Hover-triggered tilting card
- **Glowing Magic Rings** `background` — Glowing animated concentric rings
- **Gooey Glass Nav** `nav` — Gooey glass navigation bar
- **Google Reviews Live** `other` — Auto-updating Google reviews
- **Gradient Strips** `background` — Animated gradient bars for backgrounds
- **Gravity Type** `text` — Headline with falling letters
- **GravityGallery** `other` — Floating gallery with hover physics
- **Grid Tunnel** `3d` — Customizable 3D tunnel
- **Halftone Media FX** `other` — Real-time halftone and dithering system
- **Heatmap Effect** `other` — Apple-style thermal heatmap visual
- **Horizon Glow** `3d` — 3D cinematic horizon glow effect
- **Immersive Video Hero** `other` — Immersive full-bleed video hero
- **Index Grid Section** `other` — Editorial hover grid for services
- **Infnite Masorny Grid** `other` — Infinite draggable masonry grid
- **Instagram Post** `other` — Instagram post embed
- **Integrations Grid** `other` — Logo grid page with search and filters
- **Interactive Feature** `other` — Premium interactive feature showcase
- **IO Knob** `button` — Interactive rotary knob control
- **Key Stroke** `text` — Typing animation with rhythm
- **kinetic hero** `text` — Heading that moves and grabs attention
- **Kinetic Wheel** `scroll` — Smoothly curved scroll wheel
- **Layout Cards Page** `card` — Full-screen expanding grid cards
- **Light Sweep** `text` — Subtle animated shine for headlines
- **Line Stitch** `text` — Stitched text, unraveled by touch
- **Liquid Flow** `3d` — WebGL liquid image slider
- **Liquid Glass Logo** `3d` — Interactive 3D glass SVG logo
- **Liquid hover effect** `3d` — Interactive WebGL liquid ripple
- **Liquid Wave Effect** `text` — Multicolor animated headline waves
- **Live Countdown** `other` — Live countdown timer
- **Live Toasts** `other` — Live purchase notification toasts
- **Llanberis Drop** `background` — Ambient background for modern sites
- **Mask Image Reveal** `other` — Rub-and-reveal image mask
- **MasonFilter** `other` — Filterable masonry with lightbox
- **Media Gallery Slider** `other` — Modern slider with custom player
- **Media Lightbox Pro** `other` — Lightbox media gallery
- **Mega Saas Navigation** `nav` — Mega navigation designed for scale
- **Morph Pill Card** `card` — Pill morphs to card on hover
- **Motion Reveal Hero** `cursor` — Animated hero with cursor reveal
- **Neon Bloom** `background` — Neon bloom and color grade for video
- **Network Intelligence** `chart` — Animated network intelligence flow diagram
- **Newsletter Modal Pop** `other` — Modern email signup popup modal
- **Newsletter** `other` — Interactive newsletter section
- **Nice-Scroll-Progress** `scroll` — Scroll progress bar
- **One Wish Willow** `other` — Interactive wish-making easter egg
- **Open Hours** `other` — Real-time open/closed hours schedule
- **Orbit Carousel 3D** `3d` — Drag to spin a 3D gallery
- **Origin Button** `cursor` — Cursor-aware hover effect button
- **Our Approach Cards** `card` — Flip cards for step-based flows
- **Paper Flutter** `other` — Fluttering paper animation
- **Parallax Gallery** `scroll` — Horizontal parallax image gallery
- **ParallaxStack** `scroll` — Parallax slider for photos and video
- **ParallaxStoryReveal** `scroll` — Long-form editorial text parallax reveal
- **Particle Globe 3D** `3d` — Interactive 3D particle globe
- **Percent Preloader** `other` — Animated percentage preloader
- **Phantom Gallery** `other` — Image gallery component
- **Pixel Cursor Trail** `cursor` — Interactive pixel image reveal trail
- **Pixel Fire** `background` — Pixelated fire shader
- **Pixelated Canvas** `other` — Interactive pixel image effect
- **Pixora Image Trail** `cursor` — Interactive pixel image trails
- **PlanMatch** `other` — Helps visitors match needs to plans
- **Practical Doc Blocks** `other` — Content blocks for docs and guides
- **Premium SaaS Navbar** `nav` — Conversion-focused SaaS navbar
- **Prisma Nodes** `background` — Interactive fiber node network
- **Pro Text Type Effect** `text` — Pro typing text effect
- **Progressive Blur** `background` — Apple iOS-style progressive blur
- **Property Hover Card** `card` — Reveals properties on hover
- **QuoteCarousel** `other` — Animated reviews with thumbnails
- **Random Variants** `other` — Randomly swaps component variants
- **Real Shadows** `background` — Soft animated natural shadows
- **Receipt Pricing Card** `card` — Receipt-style reveal pricing card
- **Receiptgenerator** `other` — Downloadable receipt generator
- **ReelGallery** `scroll` — Infinite parallax video gallery
- **Region Based Value** `other` — Shows values based on visitor region
- **Responsive 3D Button** `cursor` — Cursor-reactive 3D button
- **Reveal Gallery Pro** `other` — Fullscreen image reveal panels
- **Rhombus Grid** `other` — Diamond grid with mobile fallback
- **Ring Image Carousel** `other` — Rotating gallery, zooms on hover
- **Ring Spiral Gallery** `3d` — 3D interactive spiral media gallery
- **Ripple Social Card** `card` — Premium glass social card
- **ROI Pricing Calc** `other` — Interactive ROI savings calculator
- **Rollover Cards** `card` — Rollover cards for feature sections
- **Interactive Balls** `3d` — Interactive 3D bouncing spheres
- **Scroll Cards Gallery** `scroll` — Scroll-animated photo and video grid
- **Scroll Expand Media** `scroll` — Media expands from ordinary to immersive on scroll
- **Scroll Images Pro** `scroll` — Scroll gallery with cinematic text
- **Scroll Reveal Paralx** `scroll` — Cinematic image reveals on scroll
- **Scroll Services** `scroll` — Sticky scroll services showcase
- **Scrolling promo bar** `scroll` — Animated announcement bar
- **Search FAQ Accordion** `other` — FAQ search with smooth accordion
- **Shader Form** `other` — Responsive native form section with shader
- **Shape Spotlight** `cursor` — Dynamic cursor glow shape effect
- **Showcase Scroll** `scroll` — Responsive storytelling showcase
- **Sinematic Scroll** `scroll` — Cinematic scroll storytelling
- **Slider-d-finity** `other` — Infinite draggable slider
- **Slot Text** `text` — Slot-machine effect for headings and links
- **Smart Spacer Pro** `other` — Pixel-perfect spacing utility
- **Smooth Scroll** `scroll` — Smooth scrolling with extra controls
- **Social Chat Widget** `button` — Floating social chat widget
- **SpatialMorphGallery** `other` — 3-in-1 gallery carousel with 2 phases
- **SpiderEffect** `background` — Crawling spider through dots
- **Spiral In** `other` — Photos spiral into a neat stack
- **Split Underline Link** `button` — Satisfying split-underline hover link
- **Stack Text Reveal** `text` — Awwwards-style text reveal effect
- **StepFlow Native** `other` — Turns native Framer forms into multi-step flows
- **Studio Navigation** `nav` — Elegant navigation bar
- **Studio Sidebar Nav** `nav` — Elegant sidebar navigation
- **SVG Hover Image Card** `card` — Stroke-sweep SVG hover image card
- **SVG Patterns** `background` — 350+ SVG background patterns
- **Switch modes** `button` — Expressive dark/light mode toggle
- **CMS Tags Organizer** `other` — Displays tags for CMS items
- **Tech Nav Bar Pro** `nav` — Premium tech navigation bar
- **Testimonial Arc Pro** `other` — Arc-shaped testimonial carousel
- **Testimonial Pro V1** `other` — Professional customer testimonials
- **Testimonial Rotation** `other` — Animated rotating testimonials
- **Testimonial Stack1** `other` — Smooth stacked testimonial slider
- **Testimonial V1** `other` — Premium testimonial carousel
- **TestimonialPill** `other` — Testimonials with expanding avatars
- **Testimonials Slider2** `other` — Testimonials slider with progress
- **Text Cylinder** `text` — Rolling text cylinder
- **Text Follow Path** `text` — Text following an SVG path
- **Text Roll Animation** `text` — Rolling text animation
- **The Split** `text` — Typography paired with a visual carousel
- **Tilt Image Carousel** `other` — CMS-powered tilting image carousel
- **True Liquid Glass** `other` — Real liquid glass effects
- **Twisted Text** `text` — Twist lines of text
- **Upflow Gallery** `3d` — Floating 3D image gallery with popup
- **Vector Globe** `3d` — Interactive vector globes
- **Reels Video Carousel** `other` — Video carousel, auto-mutes on swipe
- **Volumen Scroll Video** `scroll` — Scroll becomes the video scrubber
- **Watercolor Shader** `background` — Watercolor-inspired shader background
- **Wave Fill Preloading** `text` — Water wave fills text on load
- **Wave Gallery Page** `scroll` — Scroll-driven dual-wave gallery
- **Wax Seal** `3d` — Pour, stamp and emboss in 3D wax
- **WebGL Particle Text** `3d` — Particle text rendered in WebGL
- **WebGLCurvedCarousel** `3d` — Infinite curved WebGL carousel
