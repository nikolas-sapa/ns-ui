"use client";

import { ExtrusionDieCut } from "./component";

export default function ExtrusionDieCutDemo() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-8">
      <ExtrusionDieCut label="Exporting batch" />
    </main>
  );
}
