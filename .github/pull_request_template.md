## What this changes

<!-- One or two sentences. If it adds a component, name it and say what the
     interaction is. -->

## Checklist

- [ ] `npm run typecheck` is clean
- [ ] `npm run verify` passes (dev server running), or this change touches no components
- [ ] I looked at the resting-state screenshot in both dark and light
- [ ] Colors come from CSS custom properties, not hardcoded hex, including in canvas/SVG draw code
- [ ] No generated file is in the diff (`registry.json`, `registry/index.tsx`, `public/r/*`, `public/llms*.txt`, `lib/*.generated.json`)

## Screenshots

<!-- For a new or changed component, paste the dark and light resting-state
     screenshots that `npm run verify` produced. -->
