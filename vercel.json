{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx convex deploy --cmd 'npm run build'",
  "crons": [
    {
      "path": "/api/status-snapshot",
      "schedule": "0 6 * * *"
    }
  ]
}
